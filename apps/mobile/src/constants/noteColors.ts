// Re-export everything from shared tokens
export {
  NOTE_COLOR_PRESETS,
  resolveNoteColor,
  hasCustomColor,
  toPresetId,
  type NoteColorPreset,
} from '../../../../packages/shared/tokens/noteColors';
