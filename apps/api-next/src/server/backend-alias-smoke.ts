/**
 * Smoke import for @backend/* path alias (task 0.3).
 * Extensionless specifiers for Turbopack runtime resolution (.ts sources).
 */
export { evaluateReadiness } from "@backend/health/readiness";
export type { ReadinessStatus } from "@backend/health/readiness";