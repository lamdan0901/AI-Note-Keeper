---
phase: 05-worker-push-merge-and-throttle-hardening
plan: 04
type: execute
wave: 3
depends_on: [05-02, 05-03]
files_modified:
  - apps/backend/src/runtime/createApiServer.ts
  - apps/backend/src/worker/index.ts
  - apps/backend/src/tests/parity/phase5.http.contract.test.ts
  - apps/backend/src/tests/parity/phase5.security-boundary.test.ts
  - apps/backend/src/tests/parity/phase5.worker.contract.test.ts
autonomous: true
requirements: [JOBS-01, PUSH-01, MERG-01, THRT-01]
must_haves:
  truths:
    - "Worker runtime starts with phase-5 dispatch and push handlers active and safe under restart simulation."
    - "HTTP merge preflight/apply paths are mounted and parity-tested end-to-end through createApiServer."
    - "Security boundary tests prove throttle/lock protections and stable error envelopes across abuse and malformed-input cases."
  artifacts:
    - path: "apps/backend/src/runtime/createApiServer.ts"
      provides: "phase-5 merge route mounting in integrated API runtime"
    - path: "apps/backend/src/tests/parity/phase5.http.contract.test.ts"
      provides: "integrated parity tests for worker/push/merge/throttle behavior"
    - path: "apps/backend/src/tests/parity/phase5.security-boundary.test.ts"
      provides: "security regression suite for lock safety, throttle metadata, and unauthorized boundaries"
  key_links:
    - from: "apps/backend/src/runtime/createApiServer.ts"
      to: "apps/backend/src/merge/routes.ts"
      via: "mount /api/merge route tree"
      pattern: "app.use\('/api/merge'"
    - from: "apps/backend/src/worker/index.ts"
      to: "apps/backend/src/worker/boss-adapter.ts"
      via: "worker bootstrap uses adapter with registered phase-5 handlers"
      pattern: "createPgBossAdapter"
    - from: "apps/backend/src/tests/parity/phase5.http.contract.test.ts"
      to: "apps/backend/src/merge/service.ts"
      via: "mounted HTTP assertions for preflight/apply + rate_limit metadata"
      pattern: "retryAfterSeconds|resetAt"
---

<objective>
Wire phase-5 domains into runtime and lock the behavior with integrated parity/security suites so worker, push, merge, and throttle correctness is proven at system boundaries.

Purpose: Convert plan-level domain work into end-to-end regression gates before phase completion.
Output: Runtime wiring updates and phase-5 parity/security/worker integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-worker-push-merge-and-throttle-hardening/05-CONTEXT.md
@.planning/phases/05-worker-push-merge-and-throttle-hardening/05-02-PLAN.md
@.planning/phases/05-worker-push-merge-and-throttle-hardening/05-03-PLAN.md
@apps/backend/src/runtime/createApiServer.ts
@apps/backend/src/worker/index.ts
@apps/backend/src/tests/parity/phase4.http.contract.test.ts
@apps/backend/src/tests/parity/phase4.security-boundary.test.ts

<interfaces>
From apps/backend/src/runtime/createApiServer.ts:
```ts
app.use('/api/auth', createAuthRoutes(options.authService));
app.use('/api/reminders', createRemindersRoutes(options.remindersService));
```

From apps/backend/src/worker/index.ts:

```ts
export const startWorker = async (options: StartWorkerOptions = {}): Promise<WorkerBootstrap> => {
  const adapter = options.adapter ?? createPgBossAdapter({ logger });
  await adapter.start();
};
```

From apps/backend/src/middleware/error-middleware.ts:

```ts
const RATE_LIMIT_DETAIL_KEYS = ['retryAfterSeconds', 'resetAt'] as const;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire merge routes and phase-5 worker handlers into runtime entrypoints</name>
  <files>apps/backend/src/runtime/createApiServer.ts, apps/backend/src/worker/index.ts</files>
  <behavior>
    - Test 1: API server mounts merge routes at `/api/merge` with existing middleware ordering preserved.
    - Test 2: Worker bootstrap starts with reminder dispatch and push handlers registered.
    - Test 3: Worker start/stop remains deterministic and independent from API runtime.
  </behavior>
  <action>Mount merge routes in createApiServer without bypassing dependency gate or terminal error middleware ordering. Update worker bootstrap so phase-5 reminder dispatch and push handlers register during adapter startup, while preserving independent API/worker runtime semantics established in phase 1.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/parity/phase5.worker.contract.test.js"</automated>
  </verify>
  <done>Runtime entrypoints expose phase-5 route and worker behavior without regressing process-separation guarantees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integrated phase-5 parity HTTP contract suite</name>
  <files>apps/backend/src/tests/parity/phase5.http.contract.test.ts</files>
  <behavior>
    - Test 1: Merge preflight returns parity summary/count fields and strategy enums.
    - Test 2: Merge apply executes expected cloud/local/both outcomes with transactional guarantees.
    - Test 3: Push failure scenarios preserve retry and cleanup behavior through injected worker doubles.
  </behavior>
  <action>Create mounted HTTP parity tests using createApiServer pattern from phase-4 suites. Validate merge preflight/apply behavior, strategy enums, and expected response payloads from runtime routes. Include worker-side behavior assertions with test doubles to ensure phase-5 contracts remain stable when invoked through integration boundaries.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/parity/phase5.http.contract.test.js"</automated>
  </verify>
  <done>Phase-5 parity behavior is protected by end-to-end HTTP regression coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add phase-5 security boundary suite for throttle and lock safety</name>
  <files>apps/backend/src/tests/parity/phase5.security-boundary.test.ts, apps/backend/src/tests/parity/phase5.worker.contract.test.ts</files>
  <behavior>
    - Test 1: Repeated merge abuse attempts trigger `rate_limit` with `retryAfterSeconds` and `resetAt` metadata.
    - Test 2: Concurrent merge apply attempts do not double-write or corrupt target state.
    - Test 3: Unauthorized and malformed requests preserve stable non-2xx envelope shape.
  </behavior>
  <action>Add security regression tests that exercise anti-abuse throttle behavior, lock-safe concurrency outcomes, and envelope stability through mounted runtime routes. Include worker-contract checks that restart/retry simulation does not produce duplicate occurrence side effects.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/parity/phase5.security-boundary.test.js" ; node --test "apps/backend/dist/tests/parity/phase5.worker.contract.test.js"</automated>
  </verify>
  <done>Phase-5 security boundaries are regression-locked for throttling, locking, and envelope correctness.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                       |
| ---------------------------------------------- | --------------------------------------------------------------------------------- |
| API runtime -> merge routes                    | External clients can attempt abuse, malformed payloads, or concurrent merge races |
| worker runtime -> queue handlers               | Restart/retry behavior can duplicate execution unless idempotency safeguards hold |
| parity/security test harness -> runtime wiring | Incorrect mount order can bypass auth/dependency/error boundaries                 |

## STRIDE Threat Register

| Threat ID | Category | Component                       | Disposition | Mitigation Plan                                                                                                |
| --------- | -------- | ------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-05-14   | E        | runtime route wiring            | mitigate    | Mount merge routes behind existing dependency gate + auth middleware ordering and assert via integration tests |
| T-05-15   | D        | throttle abuse handling         | mitigate    | Security suite verifies `rate_limit` response with retry metadata and parity block windows                     |
| T-05-16   | T        | concurrent merge applies        | mitigate    | Integration tests simulate concurrent requests and assert lock-safe non-corrupt outcomes                       |
| T-05-17   | R        | worker restart/retry processing | mitigate    | Worker contract tests validate idempotent occurrence handling under restart simulation                         |

</threat_model>

<verification>
- `npm --workspace apps/backend run build`
- `node --test "apps/backend/dist/tests/parity/phase5.http.contract.test.js"`
- `node --test "apps/backend/dist/tests/parity/phase5.security-boundary.test.js"`
- `node --test "apps/backend/dist/tests/parity/phase5.worker.contract.test.js"`
</verification>

<success_criteria>

- API runtime exposes phase-5 merge routes and worker runtime boots phase-5 handlers with no lifecycle regressions.
- Integrated parity tests pass for merge strategy semantics and background behavior contracts.
- Security boundary tests pass for throttle metadata, concurrency locking, and stable error envelopes.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-worker-push-merge-and-throttle-hardening/05-04-SUMMARY.md`
</output>
