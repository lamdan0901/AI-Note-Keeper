---
phase: 05-worker-push-merge-and-throttle-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/merge/contracts.ts
  - apps/backend/src/merge/repositories/merge-repository.ts
  - apps/backend/src/merge/service.ts
  - apps/backend/src/merge/routes.ts
  - apps/backend/src/tests/merge/service.test.ts
  - apps/backend/src/tests/merge/routes.test.ts
autonomous: true
requirements: [MERG-01, MERG-02, MERG-03, THRT-01]
must_haves:
  truths:
    - 'Merge preflight returns parity summary fields and count metadata expected by clients.'
    - 'Merge apply supports cloud, local, and both strategies inside one explicit transaction.'
    - 'Concurrent merge attempts are row-lock safe and throttle to parity thresholds with retry metadata.'
  artifacts:
    - path: 'apps/backend/src/merge/service.ts'
      provides: 'preflight/apply merge behavior with lock-safe throttle and transactional persistence'
      exports: ['createMergeService']
    - path: 'apps/backend/src/merge/routes.ts'
      provides: 'HTTP merge preflight/apply endpoints with stable rate_limit envelope metadata'
      exports: ['createMergeRoutes']
    - path: 'apps/backend/src/tests/merge/service.test.ts'
      provides: 'regression coverage for transaction boundaries, locks, strategy semantics, and throttle windows'
  key_links:
    - from: 'apps/backend/src/merge/service.ts'
      to: 'packages/shared/auth/userDataMerge.ts'
      via: 'resolveMergeResolution'
      pattern: 'resolveMergeResolution'
    - from: 'apps/backend/src/merge/service.ts'
      to: 'apps/backend/src/merge/repositories/merge-repository.ts'
      via: 'row-locking reads and transactional apply writes'
      pattern: 'FOR UPDATE'
    - from: 'apps/backend/src/merge/routes.ts'
      to: 'apps/backend/src/middleware/error-middleware.ts'
      via: 'AppError rate_limit with retryAfterSeconds/resetAt'
      pattern: "code: 'rate_limit'"
---

<objective>
Implement merge preflight/apply and anti-abuse hardening in Express so concurrency, strategy semantics, and throttle responses match Convex parity contracts.

Purpose: Deliver MERG-01 through MERG-03 and THRT-01 with explicit transaction boundaries and lock-safe behavior.
Output: Merge domain contracts/repository/service/routes plus focused service and route test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-worker-push-merge-and-throttle-hardening/05-CONTEXT.md
@convex/functions/userDataMigration.ts
@tests/contract/userDataMigration.security.test.ts
@tests/contract/userDataMergeDecision.test.ts
@packages/shared/auth/userDataMerge.ts
@apps/backend/src/db/migrations/00007_migration_attempts.sql
@apps/backend/src/middleware/error-middleware.ts

<interfaces>
From packages/shared/auth/userDataMerge.ts:
```ts
export const resolveMergeResolution = (summary: MergeSummary): MergeResolution => {
  // canonical merge resolution semantics
};
```

From convex/functions/userDataMigration.ts:

```ts
type MergeStrategy = 'cloud' | 'local' | 'both';
const THROTTLE_THRESHOLD = 3;
const BASE_BLOCK_MS = 60 * 1000;
const MAX_BLOCK_MS = 15 * 60 * 1000;
```

From apps/backend/src/middleware/error-middleware.ts:

```ts
new AppError({
  code: 'rate_limit',
  details: { retryAfterSeconds, resetAt },
});
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define merge contracts and lock-capable repository interfaces (D-09, D-10, D-15)</name>
  <files>apps/backend/src/merge/contracts.ts, apps/backend/src/merge/repositories/merge-repository.ts</files>
  <behavior>
    - Test 1: Contract types expose preflight fields `sourceEmpty`, `sourceSampleOnly`, `targetEmpty`, `hasConflicts`, and source/target counts.
    - Test 2: Strategy enum is strictly `cloud | local | both` and rejects alias values.
    - Test 3: Repository API includes transaction and row-lock operations for migration-attempt and target-user rows.
  </behavior>
  <action>Create merge-domain DTO contracts and repository interfaces for preflight/apply flows. Preserve parity preflight summary fields (D-09) and keep strategy enum values unchanged as `cloud|local|both` (D-10). Define repository methods that support explicit `SELECT ... FOR UPDATE` semantics on migration-attempt and target-user rows (D-15).</action>
  <verify>
    <automated>npm --workspace apps/backend run build</automated>
  </verify>
  <done>Contracts and repository interfaces compile and encode parity strategy/summary plus lock-capable transaction primitives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional merge service with throttle parity and shared resolution semantics (D-11, D-12, D-13, D-14, D-15)</name>
  <files>apps/backend/src/merge/service.ts</files>
  <behavior>
    - Test 1: Preflight returns parity summary/count fields with conflict detection semantics.
    - Test 2: Apply executes in one explicit transaction and commits atomically per request.
    - Test 3: Strategy `both` uses canonical `resolveMergeResolution` semantics for conflict handling.
    - Test 4: Throttle key is `toUserId` and applies threshold 3, base 60s, exponential backoff capped at 15 minutes.
    - Test 5: Concurrent requests lock migration-attempt and target-user rows, preventing double-apply corruption.
  </behavior>
  <action>Implement merge service using repository transaction callbacks: acquire row-level locks (`FOR UPDATE`) for attempt and target-user rows, evaluate throttle state keyed by `toUserId` (D-14), and apply parity constants threshold=3, base block=60 seconds, max block=15 minutes (D-13). Ensure `apply` runs in a single explicit transaction (D-11). For `strategy=both`, route conflict resolution through shared `resolveMergeResolution` semantics (D-12) rather than ad-hoc merge rules.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/merge/service.test.js"</automated>
  </verify>
  <done>Merge service enforces transactional atomicity, lock safety, parity throttle constants, and canonical merge resolution behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add merge HTTP routes and error-envelope parity tests (D-09, D-10, D-16)</name>
  <files>apps/backend/src/merge/routes.ts, apps/backend/src/tests/merge/service.test.ts, apps/backend/src/tests/merge/routes.test.ts</files>
  <behavior>
    - Test 1: `/preflight` returns required parity fields and count objects.
    - Test 2: `/apply` accepts only `cloud|local|both` strategies and returns deterministic result summary.
    - Test 3: Throttle rejection returns `rate_limit` with `retryAfterSeconds` and `resetAt` metadata.
  </behavior>
  <action>Create authenticated merge routes for preflight and apply that call merge service and preserve stable AppError envelope behavior. Return `rate_limit` errors with retry metadata in details (D-16). Port contract assertions from `tests/contract/userDataMigration.security.test.ts` into backend route/service tests to lock parity semantics.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/merge/routes.test.js" ; node --test "apps/backend/dist/tests/merge/service.test.js"</automated>
  </verify>
  <done>Merge route surface is parity-compatible, strategy-safe, and security-hardened with explicit rate-limit metadata behavior.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                         |
| ----------------------------------------- | ----------------------------------------------------------------------------------- |
| client merge request -> merge routes      | Untrusted credentials, strategy, and target IDs cross into sensitive merge workflow |
| merge service -> transactional repository | Concurrent writes can race and corrupt target data without lock discipline          |
| throttle state -> error envelope          | Abuse controls must block repeated attacks without leaking internals                |

## STRIDE Threat Register

| Threat ID | Category | Component                        | Disposition | Mitigation Plan                                                                                                     |
| --------- | -------- | -------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------- |
| T-05-05   | S        | merge credential preflight/apply | mitigate    | Validate target account identity and credentials before merge apply; reject mismatches with auth/forbidden contract |
| T-05-06   | T        | merge apply transactional writes | mitigate    | Execute apply in one explicit transaction and lock target + attempt rows with FOR UPDATE (D-11, D-15)               |
| T-05-07   | D        | anti-abuse throttle path         | mitigate    | Apply parity thresholds keyed by toUserId and exponential block windows (D-13, D-14)                                |
| T-05-08   | I        | throttle response details        | mitigate    | Expose only `retryAfterSeconds` and `resetAt` via AppError rate_limit details (D-16)                                |
| T-05-09   | R        | strategy conflict resolution     | mitigate    | Use canonical shared resolveMergeResolution semantics for `both` strategy outcomes (D-12)                           |

</threat_model>

<verification>
- `npm --workspace apps/backend run build`
- `node --test "apps/backend/dist/tests/merge/service.test.js"`
- `node --test "apps/backend/dist/tests/merge/routes.test.js"`
</verification>

<success_criteria>

- Merge preflight/apply behavior matches parity summary and strategy contracts.
- Apply flow is lock-safe and transactionally atomic under concurrent requests.
- Throttle behavior matches parity constants and returns stable `rate_limit` retry metadata.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-worker-push-merge-and-throttle-hardening/05-02-SUMMARY.md`
</output>
