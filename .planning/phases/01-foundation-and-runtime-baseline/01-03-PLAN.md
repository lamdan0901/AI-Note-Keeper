---
phase: 01-foundation-and-runtime-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/migrate.ts
  - apps/backend/src/migration-tools/contracts.ts
  - apps/backend/src/migration-tools/reporting.ts
  - apps/backend/src/migration-tools/checkpoints.ts
  - apps/backend/src/migration-tools/commands/export.ts
  - apps/backend/src/migration-tools/commands/import.ts
  - apps/backend/src/migration-tools/commands/reconcile.ts
  - apps/backend/src/migration-tools/index.ts
  - apps/backend/src/tests/migration-tools.test.ts
  - apps/backend/src/tests/migration-runner.test.ts
autonomous: true
requirements:
  - BASE-02
  - BASE-07
  - SHRD-01
user_setup: []
must_haves:
  truths:
    - "Migration execution remains deterministic and safely re-runnable with schema history checks."
    - "Export/import/reconcile CLI skeletons exist with typed options and no-op adapters."
    - "Dry-run output is deterministic and emitted as both human summary and machine-readable JSON artifact."
    - "Checkpoint/resume and reconciliation report contracts are defined now, including explicit pass/fail thresholds."
    - "Migration/report placeholder semantics reuse shared utilities from packages/shared instead of backend reimplementation."
  artifacts:
    - "apps/backend/src/migrate.ts"
    - "apps/backend/src/migration-tools/contracts.ts"
    - "apps/backend/src/migration-tools/commands/export.ts"
    - "apps/backend/src/migration-tools/commands/import.ts"
    - "apps/backend/src/migration-tools/commands/reconcile.ts"
    - "apps/backend/src/tests/migration-tools.test.ts"
  key_links:
    - "apps/backend/src/migration-tools/index.ts -> apps/backend/src/migration-tools/commands/*.ts"
    - "apps/backend/src/migration-tools/reporting.ts -> packages/shared/utils/hash.ts"
    - "apps/backend/src/migrate.ts -> apps/backend/src/db/migrations/*.sql"
---

<objective>
Create the deterministic migration and tooling scaffolds required to rehearse data movement safely before parity domain phases.

Purpose: De-risk later migration execution by locking contracts, artifacts, and deterministic outputs in phase 1.
Output: Hardened migration runner semantics plus typed export/import/reconcile skeleton commands and report/checkpoint contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-runtime-baseline/01-CONTEXT.md
@apps/backend/src/migrate.ts
@apps/backend/src/db/bootstrap.ts
@apps/backend/src/db/migrations/00001_users.sql
@apps/backend/src/db/migrations/00009_core_indexes.sql
@packages/shared/utils/hash.ts

<interfaces>
From apps/backend/src/migrate.ts:
```ts
async function runMigrations() {
  const files = await fs.readdir(migrationsDir);
  const sqlFiles = files.filter((f) => f.endsWith('.sql')).sort((a, b) => a.localeCompare(b));
}
```

From apps/backend/src/db/bootstrap.ts:
```ts
export async function ensureDatabaseExists(databaseUrl: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden deterministic migration execution and re-run guarantees</name>
  <files>apps/backend/src/migrate.ts, apps/backend/src/tests/migration-runner.test.ts</files>
  <behavior>
    - Test 1: Migration ordering is deterministic and stable across repeated runs.
    - Test 2: Applied migrations are skipped safely and schema history remains consistent.
    - Test 3: Startup/runtime flow does not auto-run migrations (migrate remains explicit command).
  </behavior>
  <action>Extend migration runner tests and implementation for BASE-02, preserving deterministic sorted execution and `schema_migrations` tracking. Keep migration execution as explicit command path per D-07 and do not introduce automatic migration execution into API boot logic.</action>
  <verify>
    <automated>npm --workspace apps/backend run test</automated>
  </verify>
  <done>Migrations are demonstrably re-runnable and ordered, with explicit tests for history tracking and no implicit runtime auto-apply behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build export/import/reconcile CLI skeleton contracts and deterministic dry-run artifacts</name>
  <files>apps/backend/src/migration-tools/contracts.ts, apps/backend/src/migration-tools/reporting.ts, apps/backend/src/migration-tools/checkpoints.ts, apps/backend/src/migration-tools/commands/export.ts, apps/backend/src/migration-tools/commands/import.ts, apps/backend/src/migration-tools/commands/reconcile.ts, apps/backend/src/migration-tools/index.ts, apps/backend/src/tests/migration-tools.test.ts</files>
  <behavior>
    - Test 1: Export/import/reconcile commands parse typed options and execute no-op adapters without side effects.
    - Test 2: Dry-run emits deterministic human summary and JSON artifact from identical input.
    - Test 3: Checkpoint schema validates resume metadata and reconcile report includes pass/fail threshold fields.
  </behavior>
  <action>Implement CLI scaffolding per D-13 with typed option contracts and no-op adapters only. Implement deterministic dry-run reporters per D-14, checkpoint schema plus resume validation per D-15, and reconciliation contract with counts/checksum/sampling placeholders and explicit threshold gates per D-16. For SHRD-01, use canonical shared utilities from packages/shared (for example hashing helpers) rather than reimplementing report/checksum semantics inside backend modules.</action>
  <verify>
    <automated>npm --workspace apps/backend run test</automated>
  </verify>
  <done>Migration tooling skeleton is runnable in dry-run mode with deterministic summary+JSON output and explicit checkpoint/reconcile contracts, while reusing shared utility semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator->migration CLI | Human/operator input enters export/import/reconcile command layer. |
| migration CLI->postgres | Tooling performs privileged DB reads/writes and checkpoint handling. |
| migration report->artifact consumers | Downstream automation trusts machine-readable dry-run/report outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | T | CLI options/contracts | mitigate | Validate typed CLI options and checkpoint schema before command execution; reject invalid resume payloads early. |
| T-01-10 | R | migration artifacts | mitigate | Emit deterministic JSON artifacts and summaries so rehearsal outputs are reproducible and auditable. |
| T-01-11 | D | migration runner | mitigate | Preserve idempotent replay protections with schema_migrations tracking and deterministic ordering tests. |
| T-01-12 | I | report payloads | accept | Placeholder reconciliation metrics are non-sensitive in this phase; production data exposure controls deferred to migration execution phase. |
</threat_model>

<verification>
- Run backend suite: `npm --workspace apps/backend run test`.
- Run migration command in dry-run mode twice and compare artifact outputs for deterministic equality.
</verification>

<success_criteria>
- BASE-02 and BASE-07 are fully implemented as executable scaffolds with deterministic behavior.
- SHRD-01 is satisfied by explicit reuse of shared package utilities in migration-tooling modules.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-runtime-baseline/01-03-SUMMARY.md`
</output>
