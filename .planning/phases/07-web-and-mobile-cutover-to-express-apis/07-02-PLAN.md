---
phase: 07-web-and-mobile-cutover-to-express-apis
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - apps/web/src/main.tsx
  - apps/web/src/services/notes.ts
  - apps/web/src/services/reminders.ts
  - apps/web/src/services/subscriptions.ts
  - apps/web/src/pages/NotesPage.tsx
  - apps/web/src/App.tsx
  - apps/web/tests/notes.expressApi.test.ts
  - apps/web/tests/subscriptions.expressApi.test.ts
autonomous: true
requirements: [WEB-01, WEB-02]
must_haves:
  truths:
    - 'Web notes, reminders, and subscriptions interactions operate through Express APIs without Convex runtime dependency.'
    - 'Web cutover enforces notes sync on focus and every 30 seconds before full rollout gate can pass.'
    - 'Web flow preserves authenticated ownership semantics and stable non-2xx error handling.'
  artifacts:
    - path: 'apps/web/src/services/notes.ts'
      provides: 'Express-backed notes query/sync actions with polling-compatible fetch behavior'
    - path: 'apps/web/src/pages/NotesPage.tsx'
      provides: 'focus sync + 30 second polling loop over Express notes sync endpoint'
    - path: 'apps/web/tests/notes.expressApi.test.ts'
      provides: 'web parity tests for notes sync and polling gate behavior'
  key_links:
    - from: 'apps/web/src/pages/NotesPage.tsx'
      to: 'apps/web/src/services/notes.ts'
      via: 'focus and interval triggers invoke Express notes sync path'
      pattern: 'focus|setInterval|sync'
    - from: 'apps/web/src/main.tsx'
      to: 'apps/web/src/App.tsx'
      via: 'runtime no longer requires ConvexProvider for domain data transport'
      pattern: 'ConvexProvider'
---

<objective>
Complete web domain cutover to Express APIs and lock the polling contract gate required for production rollout.

Purpose: Fulfill WEB-01 and WEB-02 by replacing Convex domain transport and enforcing focus + 30s polling parity.
Output: Web domain services on Express APIs, polling gate wiring, and parity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-RESEARCH.md
@.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-01-PLAN.md
@apps/web/src/main.tsx
@apps/web/src/App.tsx
@apps/web/src/pages/NotesPage.tsx
@apps/web/src/services/notes.ts
@apps/web/src/services/reminders.ts
@apps/web/src/services/subscriptions.ts

<interfaces>
From apps/backend/src/notes/routes.ts:

```ts
GET /api/notes -> { notes }
POST /api/notes/sync -> { notes, deleted, conflicts }
DELETE /api/notes/:noteId -> { deleted }
```

From apps/backend/src/subscriptions/routes.ts:

```ts
GET /api/subscriptions -> { subscriptions }
POST /api/subscriptions -> { subscription }
PATCH /api/subscriptions/:subscriptionId -> { subscription }
```

From apps/backend/src/reminders/routes.ts:

```ts
GET /api/reminders -> { reminders }
POST /api/reminders -> { reminder }
PATCH /api/reminders/:reminderId -> { updated, reminder }
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace web domain services with Express API adapters</name>
  <files>apps/web/src/services/notes.ts, apps/web/src/services/reminders.ts, apps/web/src/services/subscriptions.ts</files>
  <behavior>
    - Test 1: notes service list/create/update/delete operations use Express HTTP endpoints.
    - Test 2: reminders and subscriptions services preserve existing return shapes for UI consumers.
    - Test 3: non-2xx responses surface stable message/code handling for UI error state.
  </behavior>
  <action>Remove Convex hooks from web domain services and reimplement using shared web HTTP client from plan 07-01. Preserve existing mapper outputs to avoid UI churn and maintain ownership-safe request payloads.</action>
  <verify>
    <automated>MISSING - Wave 0 must create apps/web/tests/notes.expressApi.test.ts first</automated>
  </verify>
  <done>All web domain service calls route through Express API adapters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce web polling gate with focus sync and 30 second interval</name>
  <files>apps/web/src/main.tsx, apps/web/src/pages/NotesPage.tsx, apps/web/src/App.tsx</files>
  <behavior>
    - Test 1: notes sync runs on window focus.
    - Test 2: notes sync runs every 30 seconds while app tab is active.
    - Test 3: cutover runtime no longer requires ConvexProvider for notes/reminders/subscriptions data.
  </behavior>
  <action>Wire polling behavior into notes page lifecycle and remove ConvexProvider dependency in web entrypoint for phase-7 cutover path. Keep existing auth provider and UI behavior intact.</action>
  <verify>
    <automated>npm --workspace apps/web run test -- tests/notes.expressApi.test.ts</automated>
  </verify>
  <done>WEB-02 polling gate behavior is implemented and testable in the web runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add web Express cutover regression tests</name>
  <files>apps/web/tests/notes.expressApi.test.ts, apps/web/tests/subscriptions.expressApi.test.ts</files>
  <behavior>
    - Test 1: web notes service performs sync/list operations through Express endpoints.
    - Test 2: focus and 30 second polling triggers issue expected notes sync calls.
    - Test 3: subscriptions flows still map to existing UI contract after transport change.
  </behavior>
  <action>Create parity-oriented tests that mock the HTTP layer and assert endpoint usage, polling cadence, and shape compatibility for major web flows.</action>
  <verify>
    <automated>npm --workspace apps/web run test -- tests/notes.expressApi.test.ts tests/subscriptions.expressApi.test.ts</automated>
  </verify>
  <done>Web cutover behavior is regression-protected for core notes and subscription flows.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                             |
| ------------------------------------- | --------------------------------------------------------------------------------------- | ------------------ | ----------------------------------------------------------------------------- |
| Browser domain requests to /api/notes | /api/reminders                                                                          | /api/subscriptions | Untrusted input and stale auth can produce unauthorized or malformed updates. |
| Polling loop to backend APIs          | Aggressive polling or missing backoff can cause accidental load spikes and degraded UX. |

## STRIDE Threat Register

| Threat ID | Category | Component                                  | Disposition | Mitigation Plan                                                                              |
| --------- | -------- | ------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------- |
| T-07-05   | T        | Web notes/reminders/subscriptions payloads | mitigate    | Reuse backend validation contracts and map user identity from auth token only.               |
| T-07-06   | D        | Focus + interval polling loop              | mitigate    | Constrain interval to 30 seconds and add deterministic polling tests to prevent regressions. |
| T-07-07   | R        | Web cutover gate evidence                  | mitigate    | Add explicit tests asserting focus sync and interval behavior for release sign-off.          |
| T-07-08   | I        | Error handling in UI transport             | mitigate    | Preserve stable non-2xx contract mapping and avoid exposing backend internals.               |

</threat_model>

<verification>
- npm --workspace apps/web run test -- tests/notes.expressApi.test.ts tests/subscriptions.expressApi.test.ts
- npm --workspace apps/web run lint
</verification>

<success_criteria>

- Web notes, reminders, and subscriptions no longer depend on Convex transport.
- Focus sync plus 30 second polling contract is implemented and tested.
- WEB-01 and WEB-02 are satisfied for the web surface.
  </success_criteria>

<output>
After completion, create `.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-02-SUMMARY.md`
</output>
