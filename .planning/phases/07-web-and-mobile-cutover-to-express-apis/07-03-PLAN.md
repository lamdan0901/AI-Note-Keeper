---
phase: 07-web-and-mobile-cutover-to-express-apis
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/src/api/contracts.ts
  - apps/mobile/src/api/httpClient.ts
  - apps/mobile/src/auth/httpClient.ts
  - apps/mobile/src/auth/session.ts
  - apps/mobile/src/auth/AuthContext.tsx
  - apps/mobile/tests/unit/auth.sessionLifecycle.test.ts
  - apps/mobile/tests/unit/mobileAuthHttpClient.test.ts
autonomous: true
requirements: [MOBL-02]
must_haves:
  truths:
    - 'Mobile bootstrap upgrades legacy userId-only sessions to JWT through Express auth endpoints without manual intervention.'
    - 'Mobile secure-store session handling preserves access and refresh token lifecycle continuity.'
    - 'Mobile API transport primitives expose authenticated request and refresh behavior for downstream sync adapters.'
  artifacts:
    - path: 'apps/mobile/src/api/httpClient.ts'
      provides: 'authenticated mobile HTTP transport with refresh-on-401 support'
    - path: 'apps/mobile/src/auth/AuthContext.tsx'
      provides: 'bootstrap/refresh/legacy-upgrade auth state flow for Express auth APIs'
    - path: 'apps/mobile/tests/unit/auth.sessionLifecycle.test.ts'
      provides: 'regression tests for session bootstrap and silent upgrade continuity'
  key_links:
    - from: 'apps/mobile/src/auth/AuthContext.tsx'
      to: 'apps/mobile/src/auth/httpClient.ts'
      via: 'login/register/refresh/upgrade/logout token flow'
      pattern: 'upgradeSession|refresh|saveAuthSession'
    - from: 'apps/mobile/src/auth/session.ts'
      to: 'apps/mobile/src/auth/AuthContext.tsx'
      via: 'legacy session payload loading and secure-store persistence'
      pattern: 'loadLegacySessionUpgradePayload'
---

<objective>
Harden mobile auth and transport foundation for Express cutover while preserving secure storage and silent legacy upgrade behavior.

Purpose: Satisfy MOBL-02 before migrating mobile domain sync and data APIs.
Output: Mobile API client contracts, auth bootstrap continuity wiring, and regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-auth-compatibility-and-session-continuity/02-03-SUMMARY.md
@.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-RESEARCH.md
@apps/mobile/src/auth/httpClient.ts
@apps/mobile/src/auth/session.ts
@apps/mobile/src/auth/AuthContext.tsx
@apps/mobile/tests/unit/auth.sessionLifecycle.test.ts

<interfaces>
From apps/mobile/src/auth/httpClient.ts:

```ts
export type MobileAuthHttpClient = Readonly<{
  login: (input: AuthCredentials) => Promise<MobileAuthApiSession>;
  register: (input: AuthCredentials) => Promise<MobileAuthApiSession>;
  refresh: (input: RefreshInput) => Promise<MobileAuthApiSession>;
  logout: (refreshToken?: string) => Promise<void>;
  upgradeSession: (input: UpgradeSessionInput) => Promise<MobileAuthApiSession>;
}>;
```

From apps/mobile/src/auth/session.ts:

```ts
export type AuthSession = {
  userId: string;
  username: string;
  accessToken?: string;
  refreshToken?: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define mobile Express API contracts and authenticated client primitives</name>
  <files>apps/mobile/src/api/contracts.ts, apps/mobile/src/api/httpClient.ts</files>
  <behavior>
    - Test 1: mobile client attaches bearer token for protected requests when token exists.
    - Test 2: client executes refresh callback and retries exactly once on 401.
    - Test 3: refresh failure returns normalized auth-required error to callers.
  </behavior>
  <action>Create shared mobile API contract and HTTP client modules that accept token provider and refresh callback hooks, enabling sync and domain modules to migrate from Convex without duplicating auth logic.</action>
  <verify>
    <automated>MISSING - Wave 0 must create apps/mobile/tests/unit/mobileAuthHttpClient.test.ts first</automated>
  </verify>
  <done>Mobile HTTP transport contracts are available for domain cutover tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve silent legacy bootstrap and token lifecycle in mobile auth context</name>
  <files>apps/mobile/src/auth/httpClient.ts, apps/mobile/src/auth/session.ts, apps/mobile/src/auth/AuthContext.tsx</files>
  <behavior>
    - Test 1: bootstrap upgrades legacy userId-only session through /api/auth/upgrade-session.
    - Test 2: refresh token path keeps secure-store state synchronized with latest token pair.
    - Test 3: logout still revokes current refresh token and clears local auth session.
  </behavior>
  <action>Integrate the shared mobile HTTP client into auth context bootstrap and refresh paths, preserving secure-store semantics and fallback behavior from phase 2 while removing Convex dependency for auth lifecycle operations.</action>
  <verify>
    <automated>npx jest apps/mobile/tests/unit/auth.sessionLifecycle.test.ts apps/mobile/tests/unit/mobileAuthHttpClient.test.ts</automated>
  </verify>
  <done>Mobile auth runtime maintains seamless session continuity under Express auth transport.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mobile auth transport regression tests</name>
  <files>apps/mobile/tests/unit/auth.sessionLifecycle.test.ts, apps/mobile/tests/unit/mobileAuthHttpClient.test.ts</files>
  <behavior>
    - Test 1: tokenless legacy payload upgrades successfully when upgrade endpoint responds.
    - Test 2: refresh-and-retry path resolves once and rejects on repeated 401.
    - Test 3: secure-store session write/read behavior remains stable after refresh and logout.
  </behavior>
  <action>Extend auth lifecycle tests and add dedicated mobile HTTP client unit tests that mock fetch and secure-store interactions for deterministic coverage.</action>
  <verify>
    <automated>npx jest apps/mobile/tests/unit/auth.sessionLifecycle.test.ts apps/mobile/tests/unit/mobileAuthHttpClient.test.ts</automated>
  </verify>
  <done>MOBL-02 continuity behavior is regression-protected for phase-7 cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile secure-store session to Express auth APIs | Device-held tokens can be stale or invalid during network transitions. |
| Legacy session payload to upgrade endpoint | Weak validation could allow incorrect account bootstrap mapping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | S | Legacy upgrade-session bootstrap | mitigate | Enforce userId + optional legacy token validation through auth service contract and tests. |
| T-07-10 | T | Mobile token persistence | mitigate | Keep secure-store as single source of truth and update atomically after refresh success. |
| T-07-11 | D | Repeated unauthorized retries | mitigate | Retry once on 401 with explicit failure branch to avoid retry storms. |
| T-07-12 | I | Token exposure | mitigate | Continue avoiding refresh token logging and local plain-text storage outside secure-store. |
</threat_model>

<verification>
- npx jest apps/mobile/tests/unit/auth.sessionLifecycle.test.ts apps/mobile/tests/unit/mobileAuthHttpClient.test.ts
- npm run lint
</verification>

<success_criteria>
- Mobile silent legacy upgrade path remains seamless under Express auth APIs.
- Secure-store token lifecycle remains deterministic for bootstrap, refresh, and logout.
- MOBL-02 is fully addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-03-SUMMARY.md`
</output>
