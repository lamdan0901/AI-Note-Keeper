---
phase: 07-web-and-mobile-cutover-to-express-apis
plan: 04
type: execute
wave: 2
depends_on: [07-03]
files_modified:
  - apps/mobile/App.tsx
  - apps/mobile/src/sync/fetchNotes.ts
  - apps/mobile/src/sync/syncQueueProcessor.ts
  - apps/mobile/src/sync/registerDeviceToken.ts
  - apps/mobile/src/subscriptions/service.ts
  - apps/mobile/src/voice/aiIntentClient.ts
  - apps/mobile/src/notes/realtimeService.ts
  - apps/mobile/tests/integration/offlineCreateSync.test.ts
  - apps/mobile/tests/integration/offlineDeleteSync.test.ts
  - apps/mobile/tests/unit/fetchNotes.userIdMapping.test.ts
autonomous: true
requirements: [MOBL-01]
must_haves:
  truths:
    - 'Mobile offline outbox push/pull sync runs against Express APIs without breaking LWW behavior.'
    - 'Mobile subscriptions, device token registration, and AI intent calls use Express APIs under authenticated transport.'
    - 'Mobile runtime can operate without Convex provider as primary transport while preserving offline semantics.'
  artifacts:
    - path: 'apps/mobile/src/sync/fetchNotes.ts'
      provides: 'Express notes pull adapter with user-scoped mapping and retry safety'
    - path: 'apps/mobile/src/sync/syncQueueProcessor.ts'
      provides: 'Express notes sync push pipeline for outbox batches'
    - path: 'apps/mobile/tests/integration/offlineCreateSync.test.ts'
      provides: 'offline create/sync regression coverage under Express transport'
  key_links:
    - from: 'apps/mobile/src/sync/syncQueueProcessor.ts'
      to: 'apps/mobile/src/sync/fetchNotes.ts'
      via: 'push then pull reconciliation preserving local conflict semantics'
      pattern: 'processQueue|fetchNotes'
    - from: 'apps/mobile/App.tsx'
      to: 'apps/mobile/src/sync/registerDeviceToken.ts'
      via: 'bootstrap push token registration through Express device-token route'
      pattern: 'registerDevicePushToken'
---

<objective>
Cut mobile domain transport over to Express APIs while preserving offline outbox and last-write-wins sync guarantees.

Purpose: Fulfill MOBL-01 by migrating sync and adjacent domain calls without behavioral regressions.
Output: Express-backed mobile sync/domain adapters and offline parity regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-RESEARCH.md
@.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-03-PLAN.md
@apps/mobile/App.tsx
@apps/mobile/src/sync/fetchNotes.ts
@apps/mobile/src/sync/syncQueueProcessor.ts
@apps/mobile/src/sync/registerDeviceToken.ts
@apps/mobile/src/subscriptions/service.ts
@apps/mobile/src/voice/aiIntentClient.ts

<interfaces>
From apps/backend/src/notes/routes.ts:

```ts
GET /api/notes -> { notes }
POST /api/notes/sync -> { notes, deleted, conflicts }
```

From apps/backend/src/device-tokens/routes.ts:

```ts
POST /api/device-tokens -> { token }
DELETE /api/device-tokens/:deviceId -> { deleted }
```

From apps/backend/src/ai/routes.ts:

```ts
POST /api/ai/parse-voice -> VoiceIntentResponse
POST /api/ai/clarify -> VoiceIntentResponse
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate mobile notes push/pull sync transport to Express APIs</name>
  <files>apps/mobile/src/sync/fetchNotes.ts, apps/mobile/src/sync/syncQueueProcessor.ts, apps/mobile/src/notes/realtimeService.ts</files>
  <behavior>
    - Test 1: outbox push batches call Express /api/notes/sync and mark successful records as synced.
    - Test 2: pull sync reads Express /api/notes response and keeps userId scoping rules.
    - Test 3: conflict and pending states preserve existing LWW resolution behavior.
  </behavior>
  <action>Replace ConvexHttpClient-based pull/push in sync modules with shared authenticated HTTP transport from plan 07-03. Preserve existing outbox ordering, retry policy, and local conflict protections.</action>
  <verify>
    <automated>MISSING - Wave 0 must create/extend apps/mobile/tests/integration/offlineCreateSync.test.ts and apps/mobile/tests/integration/offlineDeleteSync.test.ts first</automated>
  </verify>
  <done>Mobile notes sync pipeline runs against Express APIs with preserved offline semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate mobile subscriptions/device-token/AI clients to Express APIs</name>
  <files>apps/mobile/App.tsx, apps/mobile/src/sync/registerDeviceToken.ts, apps/mobile/src/subscriptions/service.ts, apps/mobile/src/voice/aiIntentClient.ts</files>
  <behavior>
    - Test 1: device token registration and cleanup call Express device-token endpoints.
    - Test 2: subscriptions list/mutate operations no longer depend on Convex hooks.
    - Test 3: voice parse/clarify clients call Express AI endpoints with unchanged response handling.
  </behavior>
  <action>Refactor mobile domain adapters to use Express HTTP endpoints and remove Convex domain transport assumptions from bootstrap/runtime while preserving existing UI-facing types and fallback behavior.</action>
  <verify>
    <automated>npx jest apps/mobile/tests/unit/fetchNotes.userIdMapping.test.ts apps/mobile/tests/integration/offlineCreateSync.test.ts apps/mobile/tests/integration/offlineDeleteSync.test.ts</automated>
  </verify>
  <done>Adjacent mobile domain behaviors operate through Express transport without UI contract breaks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mobile cutover regression coverage for offline and LWW parity</name>
  <files>apps/mobile/tests/integration/offlineCreateSync.test.ts, apps/mobile/tests/integration/offlineDeleteSync.test.ts, apps/mobile/tests/unit/fetchNotes.userIdMapping.test.ts</files>
  <behavior>
    - Test 1: offline create -> outbox enqueue -> Express sync -> local synced state path succeeds.
    - Test 2: offline delete path remains idempotent and reconciles correctly after reconnect.
    - Test 3: userId mismatches in pull payloads remain ignored for safety.
  </behavior>
  <action>Implement and unskip phase-critical offline integration tests for Express transport, including LWW and user ownership safeguards.</action>
  <verify>
    <automated>npx jest apps/mobile/tests/integration/offlineCreateSync.test.ts apps/mobile/tests/integration/offlineDeleteSync.test.ts apps/mobile/tests/unit/fetchNotes.userIdMapping.test.ts</automated>
  </verify>
  <done>MOBL-01 parity behavior is verified for offline and sync-critical mobile flows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile outbox payloads to Express sync endpoints | Untrusted device payloads can introduce stale or cross-user state mutations. |
| Background sync retries to backend APIs | Retry storms or partial failures can cause duplicate operations and data drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | T | Outbox sync payloads | mitigate | Preserve ownership scoping, payload-hash idempotency, and backend validation contracts. |
| T-07-14 | D | Offline retry and sync loop | mitigate | Keep bounded retry policy and ensure failed batch handling remains partial-failure safe. |
| T-07-15 | R | Mobile parity evidence | mitigate | Add integration tests that prove create/delete offline flows under Express transport. |
| T-07-16 | E | Cross-user payload injection | mitigate | Retain userId mismatch filtering in pull path and auth-scoped backend writes. |
</threat_model>

<verification>
- npx jest apps/mobile/tests/integration/offlineCreateSync.test.ts apps/mobile/tests/integration/offlineDeleteSync.test.ts apps/mobile/tests/unit/fetchNotes.userIdMapping.test.ts
- npm run lint
</verification>

<success_criteria>
- Mobile sync and adjacent domain flows no longer rely on Convex domain transport.
- Offline outbox + LWW semantics remain intact under Express APIs.
- MOBL-01 is fully addressed with regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-04-SUMMARY.md`
</output>
