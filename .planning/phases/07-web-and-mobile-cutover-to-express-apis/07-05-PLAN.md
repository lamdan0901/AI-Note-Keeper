---
phase: 07-web-and-mobile-cutover-to-express-apis
plan: 05
type: execute
wave: 3
depends_on: [07-02, 07-04]
files_modified:
  - apps/web/src/config/cutover.ts
  - apps/mobile/src/config/cutover.ts
  - tests/integration/cutover.cohort-gates.test.ts
  - docs/cutover-rollout-runbook.md
  - .planning/phases/07-web-and-mobile-cutover-to-express-apis/07-cutover-checklist.md
  - apps/web/.env.example
  - apps/mobile/.env.example
autonomous: true
requirements: [CUTV-01]
must_haves:
  truths:
    - 'Rollout progression uses explicit cohort gates and does not advance on failed parity or SLO checks.'
    - 'Rollback criteria and rehearsal steps are documented and executable before full traffic migration.'
    - 'Web and mobile cutover flags are explicit, environment-driven, and auditable.'
  artifacts:
    - path: 'apps/web/src/config/cutover.ts'
      provides: 'web cutover cohort and gate config parsing'
    - path: 'apps/mobile/src/config/cutover.ts'
      provides: 'mobile cutover cohort and gate config parsing'
    - path: 'docs/cutover-rollout-runbook.md'
      provides: 'cohort progression, parity/SLO checks, and rollback drill process'
    - path: 'tests/integration/cutover.cohort-gates.test.ts'
      provides: 'automated guard test preventing unsafe rollout progression'
  key_links:
    - from: 'tests/integration/cutover.cohort-gates.test.ts'
      to: 'apps/web/src/config/cutover.ts'
      via: 'gate evaluation asserts web cutover cannot advance on failed checks'
      pattern: 'canAdvance|parity|slo'
    - from: 'tests/integration/cutover.cohort-gates.test.ts'
      to: 'apps/mobile/src/config/cutover.ts'
      via: 'gate evaluation asserts mobile cutover cannot advance on failed checks'
      pattern: 'cohort|rollback'
---

<objective>
Add rollout governance and rollback evidence gates so client cutover proceeds by cohorts with explicit parity and SLO protection.

Purpose: Fulfill CUTV-01 with enforceable config, tests, and runbook/checklist artifacts.
Output: Cohort gate config modules, rollout guard tests, and rollback-ready runbook evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-RESEARCH.md
@.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-02-PLAN.md
@.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-04-PLAN.md
@docs/CONVEX_TO_EXPRESS_MIGRATION.md
@apps/web/.env.example
@apps/mobile/.env.example

<interfaces>
Expected cutover gate contract (new):

```ts
type CutoverGateInput = {
  cohort: string;
  parityPassed: boolean;
  sloPassed: boolean;
  rollbackDrillPassed: boolean;
};

type CutoverGateResult = {
  canAdvance: boolean;
  reason?: string;
};
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define web and mobile cutover gate configuration modules</name>
  <files>apps/web/src/config/cutover.ts, apps/mobile/src/config/cutover.ts, apps/web/.env.example, apps/mobile/.env.example</files>
  <behavior>
    - Test 1: config parsing rejects unknown cohort values and missing required gate env vars.
    - Test 2: gate evaluation returns canAdvance=false when parity or SLO checks fail.
    - Test 3: rollback drill requirement blocks advancement until explicitly satisfied.
  </behavior>
  <action>Create mirrored cutover config modules for web and mobile that parse environment flags and evaluate gate progression rules. Document required env vars in both env examples.</action>
  <verify>
    <automated>MISSING - Wave 0 must create tests/integration/cutover.cohort-gates.test.ts first</automated>
  </verify>
  <done>Cutover gate logic is explicit and shared across both client surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add automated cohort gate regression tests</name>
  <files>tests/integration/cutover.cohort-gates.test.ts</files>
  <behavior>
    - Test 1: canAdvance=true only when parityPassed, sloPassed, and rollbackDrillPassed are all true.
    - Test 2: each missing gate condition returns deterministic blocked reason.
    - Test 3: cohort ordering prevents skipping directly to full rollout.
  </behavior>
  <action>Create integration-style guard tests over new cutover config modules to prevent unsafe rollout progression logic changes.</action>
  <verify>
    <automated>npx jest tests/integration/cutover.cohort-gates.test.ts</automated>
  </verify>
  <done>Rollout gate progression is enforceably tested.</done>
</task>

<task type="auto">
  <name>Task 3: Author cutover rollout runbook and rollback drill checklist</name>
  <files>docs/cutover-rollout-runbook.md, .planning/phases/07-web-and-mobile-cutover-to-express-apis/07-cutover-checklist.md</files>
  <action>Write runbook sections for cohort progression stages, parity gate evidence, SLO thresholds, rollback trigger conditions, and drill execution steps. Add checklist fields for cohort ID, verification evidence links, incident owner, rollback command path, and sign-off timestamps.</action>
  <verify>
    <automated>rg "cohort|parity|SLO|rollback|drill|sign-off" docs/cutover-rollout-runbook.md ; rg "cohort|evidence|rollback|owner|timestamp" .planning/phases/07-web-and-mobile-cutover-to-express-apis/07-cutover-checklist.md</automated>
  </verify>
  <done>Operators have concrete rollout and rollback criteria before full traffic cutover.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                               | Description                                                          |
| ------------------------------------------------------ | -------------------------------------------------------------------- |
| Human rollout decisions to production traffic controls | Missing or ambiguous gate checks can cause unsafe traffic expansion. |
| Cohort configuration to client runtime behavior        | Invalid flag values can bypass parity/SLO checks.                    |

## STRIDE Threat Register

| Threat ID | Category | Component                             | Disposition | Mitigation Plan                                                                |
| --------- | -------- | ------------------------------------- | ----------- | ------------------------------------------------------------------------------ |
| T-07-17   | E        | Cohort progression policy             | mitigate    | Require parity, SLO, and rollback drill gates before canAdvance=true.          |
| T-07-18   | T        | Cutover env configuration             | mitigate    | Parse and validate config with fail-fast errors for invalid values.            |
| T-07-19   | R        | Rollout sign-off process              | mitigate    | Enforce checklist and runbook evidence fields with explicit operator sign-off. |
| T-07-20   | D        | Bad rollout advancing to full traffic | mitigate    | Guard with automated integration tests and deterministic blocked reasons.      |

</threat_model>

<verification>
- npx jest tests/integration/cutover.cohort-gates.test.ts
- rg "cohort|parity|SLO|rollback|drill|sign-off" docs/cutover-rollout-runbook.md
- npm run lint
</verification>

<success_criteria>

- Cohort rollout cannot advance without parity and SLO gate success plus rollback drill evidence.
- Rollback criteria are documented and rehearsable before full traffic cutover.
- CUTV-01 is fully addressed.
  </success_criteria>

<output>
After completion, create `.planning/phases/07-web-and-mobile-cutover-to-express-apis/07-05-SUMMARY.md`
</output>
