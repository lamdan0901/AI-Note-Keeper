---
phase: 08-convex-decommission-and-cleanup
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/mobile/App.tsx
  - apps/mobile/src/auth/AuthContext.tsx
  - apps/mobile/src/reminders/headless.ts
  - apps/mobile/src/reminders/ui/RescheduleOverlay.tsx
  - apps/mobile/src/screens/TrashScreen.tsx
  - apps/mobile/src/sync/fetchReminder.ts
  - apps/mobile/src/sync/noteSync.ts
  - apps/mobile/src/screens/SettingsScreen.tsx
  - apps/mobile/src/components/BottomTabBar.tsx
  - apps/mobile/src/voice/aiIntentClient.ts
  - apps/mobile/src/screens/NotesScreen.tsx
  - apps/mobile/package.json
  - apps/mobile/.env.example
  - apps/mobile/eas.json
  - tests/integration/decommission.mobile-runtime.test.ts
autonomous: true
requirements: [DECM-01]
must_haves:
  truths:
    - 'Mobile runtime no longer uses Convex clients/providers for auth, reminders, trash, or sync paths.'
    - 'Mobile UI no longer uses hasConvexClient feature gating for core flows now served by Express APIs.'
    - 'Mobile package/env/EAS config no longer requires Convex runtime keys after Stage A cleanup.'
  artifacts:
    - path: 'apps/mobile/src/auth/AuthContext.tsx'
      provides: 'Express-only auth runtime behavior without Convex fallback path'
    - path: 'apps/mobile/src/reminders/headless.ts'
      provides: 'headless reminder sync path without Convex HTTP client dependency'
    - path: 'tests/integration/decommission.mobile-runtime.test.ts'
      provides: 'mobile runtime scanner preventing Convex import/env regressions'
  key_links:
    - from: 'apps/mobile/src/screens/NotesScreen.tsx'
      to: 'apps/mobile/src/voice/aiIntentClient.ts'
      via: 'voice intent client wiring remains functional after Convex-specific type removal'
      pattern: 'VoiceIntentClient|new .*IntentClient'
    - from: 'tests/integration/decommission.mobile-runtime.test.ts'
      to: 'apps/mobile/src/**'
      via: 'forbidden import and env-key assertions'
      pattern: 'convex/|EXPO_PUBLIC_CONVEX_URL|hasConvexClient'
---

<objective>
Execute Stage A cleanup for mobile by removing remaining Convex runtime dependencies while preserving offline and auth continuity expectations.

Purpose: Implement D-05 Stage A cleanup for mobile runtime/config before Stage B disable work begins.
Output: Mobile source and configuration no longer depend on Convex runtime with guard tests preventing regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-convex-decommission-and-cleanup/08-CONTEXT.md
@.planning/phases/08-convex-decommission-and-cleanup/08-RESEARCH.md
@.planning/phases/08-convex-decommission-and-cleanup/08-01-SUMMARY.md
@apps/mobile/App.tsx
@apps/mobile/src/auth/AuthContext.tsx
@apps/mobile/src/reminders/headless.ts
@apps/mobile/src/reminders/ui/RescheduleOverlay.tsx
@apps/mobile/src/screens/TrashScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove mobile runtime Convex paths in auth/sync/reminder/trash flows (D-05 Stage A)</name>
  <files>apps/mobile/App.tsx, apps/mobile/src/auth/AuthContext.tsx, apps/mobile/src/reminders/headless.ts, apps/mobile/src/reminders/ui/RescheduleOverlay.tsx, apps/mobile/src/screens/TrashScreen.tsx, apps/mobile/src/sync/fetchReminder.ts, apps/mobile/src/sync/noteSync.ts, apps/mobile/src/screens/SettingsScreen.tsx, apps/mobile/src/components/BottomTabBar.tsx, tests/integration/decommission.mobile-runtime.test.ts</files>
  <behavior>
    - Test 1: mobile source no longer imports `convex/browser`, `convex/react`, or generated Convex API modules in active runtime paths.
    - Test 2: auth/sync flows continue with Express transport and no `hasConvexClient` gating for core screens.
    - Test 3: reminder headless and trash workflows do not require `EXPO_PUBLIC_CONVEX_URL`.
  </behavior>
  <action>Refactor active mobile runtime modules to remove Convex transport/client usage while keeping Express and local-offline behavior intact. Add an integration scan test that fails on forbidden Convex runtime imports or legacy `hasConvexClient` gate flags in mobile source files.</action>
  <verify>
    <automated>npm run -s test -- tests/integration/decommission.mobile-runtime.test.ts</automated>
  </verify>
  <done>Mobile runtime source no longer requires Convex clients or feature flags for core flows, and scanner test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete mobile config/package cleanup and voice client naming parity</name>
  <files>apps/mobile/src/voice/aiIntentClient.ts, apps/mobile/src/screens/NotesScreen.tsx, apps/mobile/package.json, apps/mobile/.env.example, apps/mobile/eas.json, tests/integration/decommission.mobile-runtime.test.ts</files>
  <behavior>
    - Test 1: notes screen still resolves a voice intent client after Convex-specific class naming is removed.
    - Test 2: mobile package/config no longer contains runtime `convex` dependency or `EXPO_PUBLIC_CONVEX_URL` env wiring.
    - Test 3: scanner test fails if `EXPO_PUBLIC_CONVEX_URL` is reintroduced to mobile env/eas config.
  </behavior>
  <action>Rename/remove Convex-specific voice client naming to transport-agnostic terminology, update NotesScreen wiring, remove `convex` from mobile dependencies, and delete `EXPO_PUBLIC_CONVEX_URL` from mobile env and EAS profiles. Keep other EAS secrets unchanged.</action>
  <verify>
    <automated>npm run -s test -- tests/integration/decommission.mobile-runtime.test.ts</automated>
  </verify>
  <done>Mobile runtime and config are Convex-free for Stage A and still satisfy existing voice/sync behavior contracts.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile runtime bootstrap to backend transport | Hidden Convex fallback paths can bypass intended Express-only decommission state. |
| Mobile build config to runtime behavior | Legacy env vars in EAS/env files can re-enable deprecated runtime paths silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | T | mobile auth/sync runtime | mitigate | Remove all Convex runtime imports in active source paths and validate with source-scan tests. |
| T-08-08 | D | mobile feature gating | mitigate | Eliminate `hasConvexClient` UI gates so core flows are not conditionally disabled after decommission. |
| T-08-09 | I | mobile config artifacts | mitigate | Remove Convex env and dependency entries from package/env/EAS files and assert absence in regression checks. |

</threat_model>

<verification>
- npm run -s test -- tests/integration/decommission.mobile-runtime.test.ts
- Select-String -Path "apps/mobile/.env.example","apps/mobile/eas.json","apps/mobile/package.json" -Pattern "EXPO_PUBLIC_CONVEX_URL|\"convex\""
</verification>

<success_criteria>
- Mobile runtime and config no longer depend on Convex imports, runtime env keys, or feature gating flags.
- Stage-A mobile cleanup remains reversible and protected by automated scanner checks.
- DECM-01 web/mobile stability precondition is preserved on mobile while Convex coupling is removed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-convex-decommission-and-cleanup/08-03-SUMMARY.md`
</output>