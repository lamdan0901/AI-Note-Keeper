---
phase: 08-convex-decommission-and-cleanup
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - apps/web/src/auth/AuthContext.tsx
  - apps/web/src/pages/reminders.tsx
  - apps/web/package.json
  - apps/web/.env.example
  - tests/integration/decommission.web-runtime.test.ts
autonomous: true
requirements: [DECM-01]
must_haves:
  truths:
    - 'Web runtime no longer imports Convex clients/hooks in active production code paths.'
    - 'Web startup and auth flows no longer require VITE_CONVEX_URL to function.'
    - 'Stage-A cleanup has automated regression coverage preventing Convex runtime reintroduction in web source.'
  artifacts:
    - path: 'apps/web/src/auth/AuthContext.tsx'
      provides: 'Express-only auth/session runtime without Convex fallback client creation'
    - path: 'apps/web/src/pages/reminders.tsx'
      provides: 'Reminders page reads data through Express transport, not Convex hooks'
    - path: 'tests/integration/decommission.web-runtime.test.ts'
      provides: 'source-level regression guard for forbidden Convex runtime imports'
  key_links:
    - from: 'apps/web/src/auth/AuthContext.tsx'
      to: 'apps/web/src/api/httpClient.ts'
      via: 'auth token resolver and refresh wiring'
      pattern: 'refresh|accessToken|apiClient'
    - from: 'tests/integration/decommission.web-runtime.test.ts'
      to: 'apps/web/src/**'
      via: 'forbidden import scanner assertion'
      pattern: 'convex/|VITE_CONVEX_URL'
---

<objective>
Execute Stage A cleanup for web by removing remaining Convex runtime coupling while preserving existing Express cutover behavior.

Purpose: Implement D-05 Stage A cleanup ordering on web surface after governance gates are in place.
Output: Web source and config no longer require Convex runtime dependencies, with regression checks that fail on reintroduction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-convex-decommission-and-cleanup/08-CONTEXT.md
@.planning/phases/08-convex-decommission-and-cleanup/08-RESEARCH.md
@.planning/phases/08-convex-decommission-and-cleanup/08-01-SUMMARY.md
@apps/web/src/auth/AuthContext.tsx
@apps/web/src/pages/reminders.tsx
@apps/web/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove web Convex runtime imports and fallback paths per Stage A cleanup (D-05)</name>
  <files>apps/web/src/auth/AuthContext.tsx, apps/web/src/pages/reminders.tsx, tests/integration/decommission.web-runtime.test.ts</files>
  <behavior>
    - Test 1: web auth bootstrap does not construct `convex/browser` clients and does not throw when `VITE_CONVEX_URL` is absent.
    - Test 2: reminders page no longer imports `convex/react` hooks or generated Convex API bindings.
    - Test 3: regression scan fails if any active web source file reintroduces `convex/*` runtime imports.
  </behavior>
  <action>Refactor remaining web Convex transport usage to existing Express API service patterns and add an integration scan test that enforces no active runtime Convex imports in `apps/web/src/**`. Keep behavioral parity with phase-7 auth refresh and reminder data flow while removing Convex-only bootstrap paths.</action>
  <verify>
    <automated>npm run -s test -- tests/integration/decommission.web-runtime.test.ts</automated>
  </verify>
  <done>Web runtime source no longer depends on Convex imports and decommission scan test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove web Convex package and env configuration surface</name>
  <files>apps/web/package.json, apps/web/.env.example</files>
  <action>Delete web `convex` runtime dependency and remove `VITE_CONVEX_URL` env documentation so web configuration reflects Express-only operation. Keep unrelated env keys untouched and ensure package scripts remain unchanged.</action>
  <verify>
    <automated>npm --workspace apps/web run build</automated>
  </verify>
  <done>Web package manifest and env example no longer advertise or require Convex runtime configuration.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser runtime to web auth/session boot | Legacy Convex fallback paths could mask missing decommission readiness and bypass intended transport behavior. |
| Source-code ownership to release artifact | Reintroduced Convex imports can silently restore old runtime coupling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T | web auth bootstrap | mitigate | Remove Convex client construction paths and assert missing `VITE_CONVEX_URL` no longer blocks startup. |
| T-08-05 | I | web reminders data flow | mitigate | Replace Convex hooks with existing Express transport wiring and keep phase-7 parity tests intact. |
| T-08-06 | D | decommission drift | mitigate | Add regression scan test that fails build on any new runtime `convex/*` web import. |

</threat_model>

<verification>
- npm run -s test -- tests/integration/decommission.web-runtime.test.ts
- npm --workspace apps/web run build
</verification>

<success_criteria>
- Web runtime operates without Convex runtime imports or `VITE_CONVEX_URL` dependency.
- Stage-A web cleanup remains reversible and fully covered by automated checks.
- DECM-01 progress is measurable through source-scan and build pass artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/08-convex-decommission-and-cleanup/08-02-SUMMARY.md`
</output>