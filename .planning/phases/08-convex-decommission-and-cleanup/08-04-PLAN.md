---
phase: 08-convex-decommission-and-cleanup
plan: 04
type: execute
wave: 3
depends_on: [08-02, 08-03]
files_modified:
  - apps/backend/src/decommission/finalizeGuard.ts
  - apps/backend/src/tests/decommission.finalizeGuard.test.ts
  - apps/backend/package.json
  - package.json
  - docs/migration-runbook.md
  - docs/cutover-rollout-runbook.md
  - docs/CONVEX_TO_EXPRESS_MIGRATION.md
  - .planning/phases/08-convex-decommission-and-cleanup/08-decommission-checklist.md
  - .planning/phases/08-convex-decommission-and-cleanup/08-final-disable-runbook.md
autonomous: false
requirements: [DECM-01]
must_haves:
  truths:
    - 'Stage B cannot proceed unless final reconcile and rollback archive artifacts are complete in the required order.'
    - 'Final shutdown instructions are explicit, operator-controlled, and linked to release-owner sign-off authority.'
    - 'Root workspace no longer carries active Convex runtime dependency after Stage A verification passes.'
  artifacts:
    - path: 'apps/backend/src/decommission/finalizeGuard.ts'
      provides: 'ordered Stage-B prerequisite validator for D-05, D-06, D-07, and D-08'
    - path: '.planning/phases/08-convex-decommission-and-cleanup/08-final-disable-runbook.md'
      provides: 'controlled operator shutdown steps with mandatory prechecks'
    - path: 'package.json'
      provides: 'workspace-level dependency cleanup after stage gate pass'
  key_links:
    - from: 'apps/backend/src/decommission/finalizeGuard.ts'
      to: '.planning/phases/08-convex-decommission-and-cleanup/08-decommission-checklist.md'
      via: 'artifact presence and sign-off field validation'
      pattern: 'pre-decommission tag|reconcile|sign-off|release owner'
    - from: '.planning/phases/08-convex-decommission-and-cleanup/08-final-disable-runbook.md'
      to: 'docs/migration-runbook.md'
      via: 'final reconcile and controlled disable sequence references'
      pattern: 'Stage B|disable Convex|rollback checkpoint'
---

<objective>
Execute Stage B finalization controls so Convex disable is only possible after verified Stage A cleanup and complete rollback/sign-off archive evidence.

Purpose: Implement D-05 through D-08 with an explicit verification gate between cleanup and final disable.
Output: Finalize guard module, controlled shutdown runbook, and final decommission checkpoint task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-convex-decommission-and-cleanup/08-CONTEXT.md
@.planning/phases/08-convex-decommission-and-cleanup/08-RESEARCH.md
@.planning/phases/08-convex-decommission-and-cleanup/08-02-SUMMARY.md
@.planning/phases/08-convex-decommission-and-cleanup/08-03-SUMMARY.md
@docs/migration-runbook.md
@docs/cutover-rollout-runbook.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement finalize guard enforcing ordered archive prerequisites (D-05, D-06, D-07)</name>
  <files>apps/backend/src/decommission/finalizeGuard.ts, apps/backend/src/tests/decommission.finalizeGuard.test.ts, apps/backend/package.json</files>
  <behavior>
    - Test 1: guard blocks Stage B when reconcile artifact is missing or older than required stage timestamp.
    - Test 2: guard blocks Stage B when pre-decommission tag, checklist artifact, or release-owner sign-off is missing.
    - Test 3: guard blocks Stage B when Stage-A verification marker is absent (D-06 verification gate).
  </behavior>
  <action>Add a backend decommission finalize guard module and CLI script that validates artifact order and completeness before any final disable instructions are considered executable. The guard must encode the required sequence from D-05 and enforce the Stage-A to Stage-B checkpoint required by D-06.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/decommission.finalizeGuard.test.js"</automated>
  </verify>
  <done>Finalize guard fails closed for incomplete/out-of-order prerequisites and exposes deterministic blocked reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Update final runbooks and root dependency surface for controlled shutdown (D-08)</name>
  <files>package.json, docs/migration-runbook.md, docs/cutover-rollout-runbook.md, docs/CONVEX_TO_EXPRESS_MIGRATION.md, .planning/phases/08-convex-decommission-and-cleanup/08-decommission-checklist.md, .planning/phases/08-convex-decommission-and-cleanup/08-final-disable-runbook.md</files>
  <action>After Stage-A summary verification, remove root runtime `convex` dependency and update migration/cutover docs with explicit controlled-shutdown operator steps, rollback checkpoints, and release-owner sign-off gates. Add `08-final-disable-runbook.md` with step-by-step Stage B execution and evidence attachment requirements (D-08).</action>
  <verify>
    <automated>Select-String -Path "docs/migration-runbook.md","docs/cutover-rollout-runbook.md",".planning/phases/08-convex-decommission-and-cleanup/08-final-disable-runbook.md" -Pattern "Stage B|controlled shutdown|release owner|disable Convex"</automated>
  </verify>
  <done>Documentation and root dependencies reflect final decommission policy with controlled Stage-B shutdown flow.</done>
</task>

 <task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Execute final operator-controlled Convex disable</name>
  <files>.planning/phases/08-convex-decommission-and-cleanup/08-decommission-checklist.md</files>
  <action>Run the controlled shutdown steps in `.planning/phases/08-convex-decommission-and-cleanup/08-final-disable-runbook.md` only after finalize guard command passes and all checklist artifacts are attached.</action>
  <instructions>
    1. Run finalize guard CLI and confirm pass.
    2. Verify checklist has pre-decommission tag, final reconcile report, and release-owner sign-off.
    3. Perform Convex disable action through operator channel documented in the runbook.
    4. Record timestamps, owner identity, and rollback reference in checklist artifact.
  </instructions>
  <verify>
    <automated>Select-String -Path ".planning/phases/08-convex-decommission-and-cleanup/08-decommission-checklist.md" -Pattern "final-disable-complete|release owner|sign-off"</automated>
  </verify>
  <done>Checklist records final disable completion metadata and release-owner sign-off evidence.</done>
  <resume-signal>Type "done" after checklist and runbook evidence are fully recorded.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stage-A summaries to Stage-B finalize gate | Missing/forged stage evidence could trigger irreversible disable too early. |
| Operator action to production Convex control plane | High-impact manual operation must be constrained by deterministic prechecks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | T | finalize guard ordering | mitigate | Enforce D-05 sequence with explicit prerequisite checks before Stage-B pass. |
| T-08-11 | R | final disable evidence trail | mitigate | Require checklist + sign-off + timestamp recording before human disable task completion. |
| T-08-12 | E | production shutdown authorization | mitigate | Gate operator disable on release-owner sign-off and finalize guard pass outcome. |

</threat_model>

<verification>
- npm --workspace apps/backend run build
- node --test "apps/backend/dist/tests/decommission.finalizeGuard.test.js"
- Select-String -Path "docs/migration-runbook.md","docs/cutover-rollout-runbook.md" -Pattern "controlled shutdown|Stage B|release owner"
</verification>

<success_criteria>
- D-05 through D-08 are represented by executable finalize guard checks and controlled operator runbook flow.
- Stage B is blocked when Stage A evidence or rollback archive artifacts are incomplete.
- Final disable step is explicitly operator-controlled and documented with sign-off evidence requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/08-convex-decommission-and-cleanup/08-04-SUMMARY.md`
</output>
