---
phase: 06-data-migration-execution-and-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/migration-tools/contracts.ts
  - apps/backend/src/migration-tools/index.ts
  - apps/backend/src/migration-tools/commands/export.ts
  - apps/backend/src/migration-tools/sources/convex-export-source.ts
  - apps/backend/src/migration-tools/sources/ordering.ts
  - apps/backend/src/tests/migration-tools.export.test.ts
  - apps/backend/package.json
autonomous: true
requirements: [MIGR-01]
must_haves:
  truths:
    - 'Running export twice against the same Convex snapshot produces identical ordered output and checksum.'
    - 'Export artifact includes deterministic metadata and per-entity counts needed by downstream import/reconcile steps.'
    - 'Export command is executable via backend scripts without manual code edits.'
  artifacts:
    - path: 'apps/backend/src/migration-tools/commands/export.ts'
      provides: 'deterministic export command execution with real source adapter'
    - path: 'apps/backend/src/migration-tools/sources/ordering.ts'
      provides: 'canonical cross-entity sort and traversal order'
    - path: 'apps/backend/src/tests/migration-tools.export.test.ts'
      provides: 'regression lock for ordering/checksum determinism'
  key_links:
    - from: 'apps/backend/src/migration-tools/commands/export.ts'
      to: 'apps/backend/src/migration-tools/sources/ordering.ts'
      via: 'entity ordering + stable per-record sort'
      pattern: 'canonicalEntityOrder|sortRecordsForExport'
    - from: 'apps/backend/src/migration-tools/index.ts'
      to: 'apps/backend/src/migration-tools/commands/export.ts'
      via: 'CLI dispatch for export'
      pattern: "case 'export'"
    - from: 'apps/backend/package.json'
      to: 'apps/backend/src/migration-tools/index.ts'
      via: 'migration-tools script'
      pattern: 'migration-tools'
---

<objective>
Implement deterministic Convex export execution so migration operators can produce repeatable artifacts with stable ordering and checksums.

Purpose: Satisfy MIGR-01 by replacing no-op export scaffolding with canonical export behavior.
Output: Real export adapter, deterministic ordering utilities, and export regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-data-migration-execution-and-reconciliation/06-RESEARCH.md
@apps/backend/src/migration-tools/contracts.ts
@apps/backend/src/migration-tools/index.ts
@apps/backend/src/migration-tools/commands/export.ts
@apps/backend/src/tests/migration-tools.test.ts
@convex/schema.ts

<interfaces>
From apps/backend/src/migration-tools/contracts.ts:
```ts
export type ExportOptions = Readonly<{
  dryRun: boolean;
  outputPath: string;
  checkpointPath?: string;
  batchSize: number;
}>;
```

From apps/backend/src/migration-tools/index.ts:

```ts
export const runMigrationToolCommand = async (
  argv: ReadonlyArray<string>,
): Promise<ToolCommandResult> => {
  /* dispatch */
};
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing export determinism tests for canonical entity ordering and checksum stability</name>
  <read_first>
    apps/backend/src/tests/migration-tools.test.ts
    apps/backend/src/migration-tools/contracts.ts
    convex/schema.ts
  </read_first>
  <files>apps/backend/src/tests/migration-tools.export.test.ts</files>
  <behavior>
    - Test 1: export orders entities exactly as `users, notes, noteChangeEvents, subscriptions, devicePushTokens, cronState, migrationAttempts, refreshTokens`.
    - Test 2: repeated export from identical source data produces byte-identical serialized payload and equal checksum.
    - Test 3: exported rows inside each entity are sorted deterministically by stable keys (`id`, fallback tuple keys for singleton tables).
  </behavior>
  <action>Create `apps/backend/src/tests/migration-tools.export.test.ts` with RED tests that currently fail against no-op export behavior. Include fixture source datasets with intentionally shuffled order and assert canonical ordering plus stable checksum equality across repeated command runs.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/migration-tools.export.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - Test file contains assertions for canonical entity order list and repeated checksum equality.
    - Command exits non-zero before implementation (RED state documented in test comments).
  </acceptance_criteria>
  <done>Determinism contract is locked in executable tests before export implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic export source adapter and canonical ordering utilities</name>
  <read_first>
    apps/backend/src/migration-tools/commands/export.ts
    apps/backend/src/migration-tools/contracts.ts
    apps/backend/src/migration-tools/reporting.ts
    apps/backend/src/tests/migration-tools.export.test.ts
  </read_first>
  <files>apps/backend/src/migration-tools/contracts.ts, apps/backend/src/migration-tools/commands/export.ts, apps/backend/src/migration-tools/sources/ordering.ts, apps/backend/src/migration-tools/sources/convex-export-source.ts</files>
  <behavior>
    - Test 1: export command calls source adapter and returns artifact data with deterministic record ordering.
    - Test 2: output metadata includes generatedAt, entity counts, batchSize, and resume cursor/checkpoint token.
    - Test 3: repeated execution over identical source returns same checksum.
  </behavior>
  <action>Replace no-op export adapter with a real source-driven pipeline. Add explicit `ExportDataset` and `ExportEntityName` contracts in `contracts.ts`. Implement `sources/ordering.ts` with canonical entity order and deterministic per-entity sort. Implement `sources/convex-export-source.ts` adapter contract that returns normalized datasets (using fixture-backed adapter in tests). Update `runExportCommand` to build artifact data from ordered records and include `entityCounts`, `batchSize`, `checkpointPath`, and `resumeToken` fields.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/migration-tools.export.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `contracts.ts` exports `ExportDataset` and `ExportEntityName`.
    - `ordering.ts` exports canonical order with exact eight-entity sequence.
    - `runExportCommand` no longer returns static `recordsScanned: 0`-only payload.
    - Export tests pass (GREEN state).
  </acceptance_criteria>
  <done>Export behavior is deterministic, contract-backed, and test-verified.</done>
</task>

<task type="auto">
  <name>Task 3: Wire backend script entrypoint for export execution and regression command path</name>
  <read_first>
    apps/backend/package.json
    apps/backend/src/migration-tools/index.ts
    apps/backend/src/tests/migration-tools.export.test.ts
  </read_first>
  <files>apps/backend/package.json, apps/backend/src/migration-tools/index.ts, apps/backend/src/tests/migration-tools.export.test.ts</files>
  <action>Add backend npm script `migration-tools` with command `npm run build && node dist/migration-tools/index.js`. Ensure CLI dispatch path supports `export --output <path> --batch-size <n> --checkpoint <path>` and include one integration-style regression test that executes `runMigrationToolCommand` with these flags and validates output metadata fields.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "apps/backend/dist/tests/migration-tools.export.test.js" ; npm --workspace apps/backend run migration-tools -- export --dry-run --output tmp/export.json --batch-size 50</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/package.json` contains `"migration-tools": "npm run build && node dist/migration-tools/index.js"`.
    - CLI regression test asserts parsed output path `tmp/export.json` and batch size `50`.
    - Export dry-run command exits 0 and prints deterministic summary/checksum.
  </acceptance_criteria>
  <done>Operators can run deterministic export tooling through stable backend script entrypoint.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                         |
| --------------------------------------------------- | ------------------------------------------------------------------- |
| migration operator CLI -> export command            | Untrusted file paths and option values enter tool boundary          |
| Convex source adapter -> serialized export artifact | Source payload ordering/shape can drift without normalization       |
| artifact metadata -> downstream import/reconcile    | Missing or unstable metadata can cause replay and mismatch failures |

## STRIDE Threat Register

| Threat ID | Category | Component                 | Disposition | Mitigation Plan                                                                       |
| --------- | -------- | ------------------------- | ----------- | ------------------------------------------------------------------------------------- |
| T-06-01   | T        | export ordering pipeline  | mitigate    | Canonical entity order + deterministic record sorting backed by RED/GREEN tests       |
| T-06-02   | R        | export artifact integrity | mitigate    | Stable serialization and checksum equality assertions across repeated runs            |
| T-06-03   | D        | CLI option parsing        | mitigate    | Explicit flag parsing tests for output/checkpoint/batch-size and fail-fast validation |
| T-06-04   | I        | dry-run output logging    | mitigate    | Keep summaries metadata-only; do not log full record payloads in command path         |

</threat_model>

<verification>
- `npm --workspace apps/backend run build`
- `node --test "apps/backend/dist/tests/migration-tools.export.test.js"`
- `npm --workspace apps/backend run migration-tools -- export --dry-run --output tmp/export.json --batch-size 50`
</verification>

<success_criteria>

- Export tooling emits deterministic ordered datasets with stable checksums across repeated runs.
- Export command is runnable from backend scripts with explicit output/checkpoint/batch-size controls.
- MIGR-01 is covered by automated regression tests.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-data-migration-execution-and-reconciliation/06-01-SUMMARY.md`
</output>
