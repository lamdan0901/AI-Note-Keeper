---
phase: 03-notes-and-adjacent-domain-api-parity
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - apps/backend/src/runtime/createApiServer.ts
  - apps/backend/src/tests/parity/phase3.http.contract.test.ts
  - apps/backend/src/tests/parity/phase3.security-boundary.test.ts
autonomous: true
requirements:
  - NOTE-01
  - SUBS-01
  - DEVC-01
  - AICP-03
user_setup: []
must_haves:
  truths:
    - 'All phase-3 domain routes are reachable under `/api/*` with authentication and stable error contracts.'
    - 'Route integration does not regress auth/session continuity from Phase 2.'
    - 'Phase-3 HTTP parity tests verify notes replay safety, subscription/device ownership, and AI fallback/rate-limit behavior end to end.'
  artifacts:
    - 'apps/backend/src/runtime/createApiServer.ts'
    - 'apps/backend/src/tests/parity/phase3.http.contract.test.ts'
    - 'apps/backend/src/tests/parity/phase3.security-boundary.test.ts'
  key_links:
    - 'apps/backend/src/runtime/createApiServer.ts -> apps/backend/src/notes/routes.ts'
    - 'apps/backend/src/runtime/createApiServer.ts -> apps/backend/src/subscriptions/routes.ts'
    - 'apps/backend/src/runtime/createApiServer.ts -> apps/backend/src/device-tokens/routes.ts'
    - 'apps/backend/src/runtime/createApiServer.ts -> apps/backend/src/ai/routes.ts'
---

<objective>
Integrate and verify all phase-3 domains inside the API runtime with parity and security boundary tests.

Purpose: Ensure phase-3 modules work together under authenticated routing and produce verifiable HTTP parity behavior.
Output: API server route wiring and phase-level parity/security regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-notes-and-adjacent-domain-api-parity/03-CONTEXT.md
@.planning/phases/03-notes-and-adjacent-domain-api-parity/03-RESEARCH.md
@apps/backend/src/runtime/createApiServer.ts
@apps/backend/src/auth/routes.ts
@apps/backend/src/notes/routes.ts
@apps/backend/src/subscriptions/routes.ts
@apps/backend/src/device-tokens/routes.ts
@apps/backend/src/ai/routes.ts
@apps/backend/src/middleware/error-middleware.ts
@tests/contract/notes.crud.test.ts
@tests/contract/subscriptions.reminders.test.ts
@tests/contract/aiNoteCapture.contract.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount phase-3 domain routes under API runtime with auth and dependency gate consistency</name>
  <files>apps/backend/src/runtime/createApiServer.ts</files>
  <read_first>apps/backend/src/runtime/createApiServer.ts, apps/backend/src/auth/routes.ts, apps/backend/src/notes/routes.ts, apps/backend/src/subscriptions/routes.ts, apps/backend/src/device-tokens/routes.ts, apps/backend/src/ai/routes.ts</read_first>
  <behavior>
    - Test 1: Notes/subscriptions/device-tokens/ai route trees are mounted under `/api` and reachable.
    - Test 2: Existing auth route behavior remains unchanged after mounting new routes.
    - Test 3: All new routes run through existing dependency gate and error middleware stack.
  </behavior>
  <action>Update API server wiring to mount phase-3 routers under `/api/notes`, `/api/subscriptions`, `/api/device-tokens`, and `/api/ai` while preserving existing `/api/auth` behavior and middleware order. Do not bypass `createDependencyGate`, `notFoundMiddleware`, or `errorMiddleware`. Ensure mounted routers require access-token middleware internally and expose stable error contracts.
</action>
  <acceptance_criteria>
    - `apps/backend/src/runtime/createApiServer.ts` contains mounts for all four phase-3 route groups.
    - `/api/auth` route mount remains present and unmodified in behavior.
    - Middleware order still includes dependency gate before domain routes and error middleware last.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/backend run test</automated>
  </verify>
  <done>API runtime serves all phase-3 domains with preserved middleware and auth behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add phase-3 HTTP parity integration suite</name>
  <files>apps/backend/src/tests/parity/phase3.http.contract.test.ts</files>
  <read_first>apps/backend/src/tests/auth/routes.test.ts, tests/contract/notes.crud.test.ts, tests/contract/subscriptions.reminders.test.ts, tests/contract/aiNoteCapture.contract.test.ts</read_first>
  <behavior>
    - Test 1: Notes sync replay with same payloadHash is idempotent over HTTP.
    - Test 2: Subscriptions and device-token mutations reject cross-user ownership violations.
    - Test 3: AI parse/clarify returns deterministic fallback output when provider unavailable and enforces rate-limit contract.
  </behavior>
  <action>Create end-to-end HTTP parity tests that start `createApiServer` with service doubles/fixtures and exercise representative phase-3 flows through real route handlers. Include notes replay scenario, subscription lifecycle scenario, device token upsert/delete idempotency scenario, and AI fallback plus rate-limit scenario. Assertions must validate stable error envelope fields (`code`, `message`, `status`) for all failure-path checks.
</action>
  <acceptance_criteria>
    - `apps/backend/src/tests/parity/phase3.http.contract.test.ts` contains test cases for notes replay idempotency, subscription/device ownership, and AI fallback/rate limiting.
    - Failure-path assertions verify exact error envelope shape fields.
    - Test suite executes through HTTP route handlers, not direct service function calls.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/backend run test</automated>
  </verify>
  <done>Phase-3 parity behavior is verified end-to-end through mounted Express routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add security-boundary regression tests for phase-3 APIs</name>
  <files>apps/backend/src/tests/parity/phase3.security-boundary.test.ts</files>
  <read_first>.planning/phases/03-notes-and-adjacent-domain-api-parity/03-CONTEXT.md, apps/backend/src/runtime/createApiServer.ts, apps/backend/src/tests/parity/phase3.http.contract.test.ts</read_first>
  <behavior>
    - Test 1: Unauthorized requests to phase-3 domain routes return auth error contract.
    - Test 2: Malformed request payloads return validation contract with issue details.
    - Test 3: Backend does not expose `notification_ledger` route/table/repository symbols.
  </behavior>
  <action>Add focused regression tests for trust boundaries: unauthenticated requests, invalid payloads, and explicit `notification_ledger` non-exposure assertions per D-11. Reuse API server test harness to keep checks at HTTP boundary. Include one static assertion scanning route registrations for forbidden `notification-ledger` path exposure.
</action>
  <acceptance_criteria>
    - `apps/backend/src/tests/parity/phase3.security-boundary.test.ts` contains auth and validation boundary tests for all phase-3 route groups.
    - Test file includes explicit assertion that `notification_ledger` is absent from backend-exposed route names.
    - Security tests run in same automated command as backend suite.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/backend run test</automated>
  </verify>
  <done>Security and boundary regressions are explicitly guarded before phase verification.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                   |
| --------------------------------- | --------------------------------------------------------------------------------------------- |
| route mounting->global middleware | Incorrect mount order can bypass dependency or error handling protections.                    |
| HTTP tests->phase behavior        | Missing integration tests can hide broken parity wiring despite passing unit tests.           |
| API surface->forbidden domains    | Accidental route exposure (for example notification_ledger) can violate boundary constraints. |

## STRIDE Threat Register

| Threat ID | Category | Component                    | Disposition | Mitigation Plan                                                                                                |
| --------- | -------- | ---------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- |
| T-03-13   | E        | API route registration       | mitigate    | Mount all phase-3 routes after dependency gate and keep authentication within each domain router.              |
| T-03-14   | R        | parity verification coverage | mitigate    | Add explicit HTTP integration scenarios for replay, fallback, and ownership failures.                          |
| T-03-15   | I        | forbidden backend exposure   | mitigate    | Add regression tests that fail on `notification_ledger` route/table/repository exposure.                       |
| T-03-16   | D        | middleware bypass            | mitigate    | Preserve existing notFound/error middleware ordering and assert auth/validation failures in integration tests. |

</threat_model>

<verification>
- Run `npm --workspace apps/backend run test` after each task.
- Confirm mounted routes include all phase-3 domains and preserve `/api/auth` behavior.
- Confirm parity/security suites pass with deterministic fallback and ownership boundary assertions.
</verification>

<success_criteria>

- Phase-3 route integration is complete and verified under real HTTP handlers.
- Auth, validation, and forbidden-surface boundaries are regression-protected.
- Phase 3 is ready for execute-phase with deterministic verification signals.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-notes-and-adjacent-domain-api-parity/03-04-SUMMARY.md`
</output>
