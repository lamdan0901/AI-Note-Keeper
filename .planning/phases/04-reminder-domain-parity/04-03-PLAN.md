---
phase: 04-reminder-domain-parity
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - apps/backend/src/tests/parity/phase4.http.contract.test.ts
  - apps/backend/src/tests/parity/phase4.security-boundary.test.ts
autonomous: true
requirements: [REMD-01, REMD-02, REMD-03, REMD-04, REMD-05]
must_haves:
  truths:
    - 'Integrated HTTP reminder behavior stays parity-correct for CRUD, ack, snooze, and updatedSince sync behavior.'
    - 'Security boundaries reject unauthorized and malformed reminder requests with stable error contracts.'
    - 'Reminder change-event dedupe and no-op mutation behavior is preserved end-to-end at HTTP surface.'
  artifacts:
    - path: 'apps/backend/src/tests/parity/phase4.http.contract.test.ts'
      provides: 'Integrated parity assertions for reminder lifecycle semantics'
    - path: 'apps/backend/src/tests/parity/phase4.security-boundary.test.ts'
      provides: 'Auth/validation/ownership boundary assertions for reminder routes'
  key_links:
    - from: 'apps/backend/src/tests/parity/phase4.http.contract.test.ts'
      to: 'apps/backend/src/runtime/createApiServer.ts'
      via: 'mounted route integration harness'
      pattern: 'createApiServer'
    - from: 'apps/backend/src/tests/parity/phase4.http.contract.test.ts'
      to: 'apps/backend/src/reminders/routes.ts'
      via: 'HTTP calls through /api/reminders'
      pattern: 'api/reminders'
    - from: 'apps/backend/src/tests/parity/phase4.security-boundary.test.ts'
      to: 'apps/backend/src/middleware/error-middleware.ts'
      via: 'asserting stable error envelope fields'
      pattern: 'code|message|status'
---

<objective>
Lock phase-4 reminder parity with integrated HTTP and security regression suites before execution advances to worker/push hardening.

Purpose: Catch behavioral drift for reminder lifecycle and dedupe semantics at the mounted API boundary.
Output: Phase-4 parity and security test suites runnable via backend node:test pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reminder-domain-parity/04-CONTEXT.md
@apps/backend/src/runtime/createApiServer.ts
@apps/backend/src/reminders/routes.ts
@apps/backend/src/tests/parity/phase3.http.contract.test.ts
@apps/backend/src/tests/parity/phase3.security-boundary.test.ts
@tests/contract/reminders.crud.test.ts
@tests/contract/reminders.list.test.ts
@tests/contract/reminders.update.test.ts
@tests/contract/reminders.ackReminder.test.ts
@tests/contract/reminders.snoozeReminder.test.ts
@tests/integration/reminders.concurrentEdits.test.ts

<interfaces>
From tests/contract/reminders.ackReminder.test.ts:
```ts
ackReminder(args: {
  id: string;
  ackType: 'done' | 'snooze';
  optimisticNextTrigger?: number;
  deviceId?: string;
})
```

From tests/contract/reminders.snoozeReminder.test.ts:

```ts
snoozeReminder(args: { id: string; snoozedUntil: number; deviceId?: string })
```

From tests/contract/reminders.list.test.ts:

```ts
listReminders(args?: { updatedSince?: number })
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phase-4 integrated HTTP parity suite for reminder lifecycle semantics (D-01 to D-03, D-05 to D-09, D-12 to D-23)</name>
  <files>apps/backend/src/tests/parity/phase4.http.contract.test.ts</files>
  <behavior>
    - Test 1: CRUD/list/get/update/delete flows preserve auth ownership and parity missing semantics.
    - Test 2: Ack recurring and one-time reminders follow transition matrix (nextTrigger/scheduleStatus/done/lastAcknowledgedAt/lastFiredAt).
    - Test 3: Snooze patch updates due state deterministically and preserves recurrence definition fields.
    - Test 4: Stale/equal timestamp updates are no-op and do not append change events.
    - Test 5: Recurrence definition edits recompute nextTrigger via shared utility semantics and remain DST-safe at contract level.
  </behavior>
  <action>Create integration harness via `createApiServer` and reminder service doubles or test repositories. Mirror proven structure from `phase3.http.contract.test.ts`, but assert phase-4 specific behaviors and dedupe semantics. Include assertions that effective writes append one event and invoke immediate post-change hook while stale/no-op writes do not.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "dist/tests/parity/phase4.http.contract.test.js"</automated>
  </verify>
  <done>Phase-4 reminder parity is covered end-to-end at HTTP boundary with deterministic assertions for lifecycle and dedupe behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add phase-4 reminder security-boundary suite for auth, validation, and contract safety (D-04, D-10, D-11)</name>
  <files>apps/backend/src/tests/parity/phase4.security-boundary.test.ts</files>
  <behavior>
    - Test 1: Unauthorized reminder endpoints return stable auth envelope.
    - Test 2: Malformed timezone/repeat/action payloads return validation envelope with issue details.
    - Test 3: Cross-user mutation attempts are rejected and cannot affect foreign reminder rows.
    - Test 4: Mounted reminder routes preserve stable error contract shape for non-2xx responses.
  </behavior>
  <action>Add security regressions analogous to phase-3 boundary tests, focused on reminder endpoints only. Verify token-derived identity enforcement and absence of body-userId trust. Validate that parity-required 200 missing responses remain explicit and do not degrade security posture.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "dist/tests/parity/phase4.security-boundary.test.js"</automated>
  </verify>
  <done>Reminder API boundary protections are regression-locked for auth, input validation, and ownership safety.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                   |
| ----------------------------------------------- | ----------------------------------------------------------------------------- |
| test harness -> mounted runtime                 | Must verify real middleware/route behavior, not isolated function assumptions |
| parity assertions -> reminder mutation outcomes | Regression coverage must detect semantic drift in lifecycle transitions       |
| security assertions -> error contracts          | Non-2xx envelopes must remain stable for clients and observability            |

## STRIDE Threat Register

| Threat ID | Category | Component                       | Disposition | Mitigation Plan                                                                                             |
| --------- | -------- | ------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------- |
| T-04-11   | T        | Reminder lifecycle semantics    | mitigate    | Add integrated parity tests for ack/snooze/recurrence/LWW/no-op paths anchored to canonical contract suites |
| T-04-12   | S        | Reminder endpoint auth boundary | mitigate    | Explicit unauthorized and cross-user mutation tests across all reminder actions                             |
| T-04-13   | R        | Error contract stability        | mitigate    | Assert `code/message/status` envelope on auth/validation/not-found style paths                              |
| T-04-14   | I        | updatedSince/list exposure      | mitigate    | Verify list/get outputs are user-scoped and do not leak cross-user reminders                                |
| T-04-15   | D        | Regression blind spots          | mitigate    | Execute parity + security suites together as release gate for phase-4 completion                            |

</threat_model>

<verification>
- `npm --workspace apps/backend run build`
- `node --test "dist/tests/parity/phase4.http.contract.test.js"`
- `node --test "dist/tests/parity/phase4.security-boundary.test.js"`
</verification>

<success_criteria>

- Phase-4 reminder parity tests pass and capture CRUD/ack/snooze/updatedSince/dedupe semantics.
- Security boundary tests pass for auth, ownership, and validation behavior.
- Reminder regression suites are runnable as backend test gate before phase closure.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-reminder-domain-parity/04-03-SUMMARY.md`
</output>
