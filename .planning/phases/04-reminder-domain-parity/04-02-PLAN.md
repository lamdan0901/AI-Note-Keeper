---
phase: 04-reminder-domain-parity
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/backend/src/reminders/routes.ts
  - apps/backend/src/runtime/createApiServer.ts
  - apps/backend/src/tests/reminders/routes.test.ts
autonomous: true
requirements: [REMD-01, REMD-02, REMD-03, REMD-04]
must_haves:
  truths:
    - "Authenticated users can list/get/create/update/delete/ack/snooze reminders via /api/reminders."
    - "Missing reminder operations return parity-style 200 responses with nullable/boolean outcomes (not forced 404s)."
    - "Routes never trust body userId and always scope operations to token identity."
  artifacts:
    - path: "apps/backend/src/reminders/routes.ts"
      provides: "Reminder HTTP contract and request validation"
      exports: ["createRemindersRoutes"]
    - path: "apps/backend/src/runtime/createApiServer.ts"
      provides: "Mounted /api/reminders route tree"
    - path: "apps/backend/src/tests/reminders/routes.test.ts"
      provides: "Route-level parity and auth/validation coverage"
  key_links:
    - from: "apps/backend/src/runtime/createApiServer.ts"
      to: "apps/backend/src/reminders/routes.ts"
      via: "app.use('/api/reminders', createRemindersRoutes(...))"
      pattern: "api/reminders"
    - from: "apps/backend/src/reminders/routes.ts"
      to: "apps/backend/src/auth/access-middleware.ts"
      via: "requireAccessUser"
      pattern: "requireAccessUser"
    - from: "apps/backend/src/reminders/routes.ts"
      to: "apps/backend/src/reminders/service.ts"
      via: "route handlers calling service methods"
      pattern: "service\.(listReminders|getReminder|createReminder|updateReminder|deleteReminder|ackReminder|snoozeReminder)"
---

<objective>
Expose reminder-domain parity behavior through authenticated Express routes and runtime mounting, including nullable/boolean missing-record response semantics.

Purpose: Deliver D-01 through D-04 route contracts without changing service semantics from plan 04-01.
Output: Reminder route module, runtime mount, and route-level regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reminder-domain-parity/04-CONTEXT.md
@apps/backend/src/auth/access-middleware.ts
@apps/backend/src/middleware/validate.ts
@apps/backend/src/runtime/createApiServer.ts
@apps/backend/src/notes/routes.ts
@apps/backend/src/tests/notes/routes.test.ts
@apps/backend/src/reminders/service.ts

<interfaces>
From apps/backend/src/auth/access-middleware.ts:
```ts
export type AuthenticatedRequest = Request & {
  authUser: Readonly<{ userId: string; username: string }>;
};
export const requireAccessUser: (deps?: AccessMiddlewareDeps) => RequestHandler;
```

From apps/backend/src/runtime/createApiServer.ts:

```ts
export type ApiServerFactoryOptions = Readonly<{
  authService?: AuthService;
  notesService?: NotesService;
  subscriptionsService?: SubscriptionsService;
  deviceTokensService?: DeviceTokensService;
  aiService?: AiService;
  aiRateLimiter?: AiRateLimiter;
}>;
```

From apps/backend/src/notes/routes.ts (pattern to mirror):

```ts
router.post('/sync', requireAccessUser(), validateRequest({ body: syncBodySchema }), ...)
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement reminder route contract with auth-first validation and parity missing semantics (D-01, D-02, D-03, D-04, D-10, D-11)</name>
  <files>apps/backend/src/reminders/routes.ts</files>
  <behavior>
    - Test 1: `/api/reminders` list supports optional `updatedSince` and only returns authenticated user scope.
    - Test 2: get/update/delete/ack/snooze on missing reminder returns HTTP 200 with `reminder: null` or `{ updated: false }`/`{ deleted: false }` style outputs.
    - Test 3: Body `userId` is ignored/rejected as authority; token userId is always used.
    - Test 4: Invalid timezone payload returns validation contract and does not invoke service mutation.
  </behavior>
  <action>Create `createRemindersRoutes` with endpoints under `/` and `/:reminderId/*` for list/get/create/update/delete/ack/snooze. Apply `requireAccessUser` and zod schemas on every mutation route. Preserve parity nullable/boolean response shape for missing reminder operations (D-02), and enforce optional `updatedSince` query filtering contract (D-03). Do not trust request body `userId`; route should always inject token user into service input (D-04).</action>
  <verify>
    <automated>MISSING - Wave 0 must create apps/backend/src/tests/reminders/routes.test.ts first</automated>
  </verify>
  <done>Reminder routes expose complete phase-4 HTTP surface with auth-scoped identity and parity-compatible missing-record behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Mount reminder routes in API runtime (D-01)</name>
  <files>apps/backend/src/runtime/createApiServer.ts</files>
  <action>Extend API server factory options with optional reminder service dependency and mount `createRemindersRoutes(...)` at `/api/reminders` after dependency gate and before notFound/error middleware. Keep existing middleware ordering and route-mount patterns unchanged.</action>
  <verify>
    <automated>npm --workspace apps/backend run build</automated>
  </verify>
  <done>`/api/reminders` is available in runtime with consistent middleware stack and no regression to existing domain mounts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add route-level reminder parity tests for auth, validation, and missing semantics</name>
  <files>apps/backend/src/tests/reminders/routes.test.ts</files>
  <behavior>
    - Test 1: Unauthorized reminder endpoints return auth error envelope.
    - Test 2: Missing reminder get/update/delete/ack/snooze returns parity 200 nullable/boolean payloads.
    - Test 3: `updatedSince` list query forwards and filters correctly.
    - Test 4: Body userId tampering cannot mutate another user's reminder.
  </behavior>
  <action>Create node:test route suite using in-memory reminder service double (pattern from notes route tests). Validate response contracts, validation failures, and token-scoped ownership semantics. Include at least one case confirming stale/equal update returns current persisted reminder payload and does not report mutation success.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "dist/tests/reminders/routes.test.js"</automated>
  </verify>
  <done>Reminder route behavior is regression-covered and aligned with phase-4 parity decisions.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                    |
| ---------------------------------------- | ------------------------------------------------------------------------------ |
| HTTP client -> reminder routes           | Untrusted IDs, timestamps, timezone strings, and ack/snooze payloads enter API |
| reminder routes -> reminder service      | Route must preserve user scoping and validated DTOs                            |
| runtime router mount -> middleware chain | Misordered mounts can bypass auth/validation/error normalization               |

## STRIDE Threat Register

| Threat ID | Category | Component                          | Disposition | Mitigation Plan                                                                                                  |
| --------- | -------- | ---------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------- |
| T-04-06   | S        | `/api/reminders/*`                 | mitigate    | Enforce `requireAccessUser` on all reminder endpoints and derive userId from token only (D-04)                   |
| T-04-07   | T        | Update/ack/snooze request payloads | mitigate    | Validate with zod schemas, including timezone/repeat constraints from contracts (D-10, D-11, D-22)               |
| T-04-08   | R        | Missing-resource path              | mitigate    | Return explicit parity payloads (`null`/`false`) with stable envelope instead of implicit branch behavior (D-02) |
| T-04-09   | I        | List endpoint scoping              | mitigate    | Apply authenticated user filtering and never expose cross-user reminders on list/get (D-03, D-04)                |
| T-04-10   | E        | API runtime mounting               | mitigate    | Mount reminder routes inside existing `/api` dependency gate and keep terminal error middleware ordering         |

</threat_model>

<verification>
- `npm --workspace apps/backend run build`
- `node --test "dist/tests/reminders/routes.test.js"`
</verification>

<success_criteria>

- `/api/reminders` route tree is mounted and authenticated.
- Missing reminder operations return parity-compatible 200 responses.
- Request validation blocks malformed timezone and action payloads.
- User identity is token-derived across all reminder mutations.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-reminder-domain-parity/04-02-SUMMARY.md`
</output>
