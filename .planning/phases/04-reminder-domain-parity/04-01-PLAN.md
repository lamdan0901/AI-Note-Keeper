---
phase: 04-reminder-domain-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/reminders/contracts.ts
  - apps/backend/src/reminders/repositories/reminders-repository.ts
  - apps/backend/src/reminders/service.ts
  - apps/backend/src/tests/reminders/service.test.ts
autonomous: true
requirements: [REMD-02, REMD-03, REMD-04, REMD-05]
must_haves:
  truths:
    - "Acknowledging recurring reminders advances next occurrence using shared recurrence semantics."
    - "Snooze deterministically updates due-state fields without mutating recurrence definition fields."
    - "Reminder change events are deduped on noteId/userId/operation/payloadHash and only emitted for effective state changes."
  artifacts:
    - path: "apps/backend/src/reminders/service.ts"
      provides: "Reminder lifecycle transitions and dedupe-aware event writes"
      exports: ["createRemindersService"]
    - path: "apps/backend/src/reminders/contracts.ts"
      provides: "Reminder DTOs, timezone validation helpers, canonical recurrence patch semantics"
      exports: ["reminderRepeatSchema", "toReminderPatch", "isValidIanaTimezone"]
    - path: "apps/backend/src/tests/reminders/service.test.ts"
      provides: "Regression coverage for ack/snooze/LWW/dedupe/no-op semantics"
  key_links:
    - from: "apps/backend/src/reminders/service.ts"
      to: "packages/shared/utils/recurrence.ts"
      via: "computeNextTrigger"
      pattern: "computeNextTrigger"
    - from: "apps/backend/src/reminders/service.ts"
      to: "packages/shared/utils/hash.ts"
      via: "calculatePayloadHash"
      pattern: "calculatePayloadHash"
    - from: "apps/backend/src/reminders/service.ts"
      to: "apps/backend/src/notes/repositories/note-change-events-repository.ts"
      via: "isDuplicate + appendEvent"
      pattern: "noteChangeEventsRepository\.(isDuplicate|appendEvent)"
---

<objective>
Implement reminder-domain core behavior so recurring ack/snooze transitions, timezone handling, and change-event dedupe match Convex parity semantics.

Purpose: Deliver D-05 through D-23 behavior without route/mounting coupling so later plans can wire HTTP safely.
Output: Reminder contracts, repository, service, and focused service tests for deterministic transition rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reminder-domain-parity/04-CONTEXT.md
@convex/functions/reminders.ts
@packages/shared/utils/recurrence.ts
@packages/shared/utils/hash.ts
@apps/backend/src/notes/service.ts
@apps/backend/src/notes/repositories/notes-repository.ts
@apps/backend/src/notes/repositories/note-change-events-repository.ts

<interfaces>
From apps/backend/src/notes/repositories/note-change-events-repository.ts:
```ts
export type NoteChangeEventsRepository = Readonly<{
  isDuplicate: (input: NoteChangeEventLookupInput) => Promise<boolean>;
  appendEvent: (
    input: NoteChangeEventLookupInput & Readonly<{ deviceId: string; changedAt?: Date }>,
  ) => Promise<void>;
}>;
```

From apps/backend/src/notes/repositories/notes-repository.ts:

```ts
export type NotePatchInput = {
  repeat?: Record<string, unknown> | null;
  startAt?: Date | null;
  baseAtLocal?: string | null;
  nextTriggerAt?: Date | null;
  lastFiredAt?: Date | null;
  lastAcknowledgedAt?: Date | null;
  timezone?: string | null;
  updatedAt?: Date;
};
```

From packages/shared/utils/recurrence.ts:

```ts
export function computeNextTrigger(
  now: number,
  startAt: number,
  baseAtLocal: string,
  repeat: RepeatRule | null,
  timezone?: string,
): number | null;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define reminder contracts and repository interfaces (D-09, D-10, D-11, D-20, D-21, D-22, D-23)</name>
  <files>apps/backend/src/reminders/contracts.ts, apps/backend/src/reminders/repositories/reminders-repository.ts</files>
  <action>Create reminder-domain contracts and schemas that preserve omitted-vs-null semantics for `repeat`, `startAt`, `baseAtLocal`, `nextTriggerAt`, `lastFiredAt`, and `lastAcknowledgedAt` (per D-20 and D-21). Add strict `repeat` rule validation (no partial repeat payloads, per D-22). Add IANA timezone validator helper for create/update flows (D-10 and D-11). Define repository signatures for list/get/create/update/delete scoped by authenticated user and update conflict gate support.</action>
  <verify>
    <automated>npm --workspace apps/backend run build</automated>
  </verify>
  <done>Contracts enforce repeat/timezone rules and repository interfaces compile with no implicit `any`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reminder service transitions, LWW gate, recurrence recompute, and change-event dedupe (D-05 to D-08, D-13 to D-19, D-23)</name>
  <files>apps/backend/src/reminders/service.ts</files>
  <behavior>
    - Test 1: Recurring `ack(done)` sets `done=true`, updates `lastAcknowledgedAt`, recomputes `nextTriggerAt` via shared `computeNextTrigger`, and unschedules when series ends.
    - Test 2: One-time `ack(done)` honors future `snoozedUntil` before unscheduling.
    - Test 3: Snooze sets `snoozedUntil`, `nextTriggerAt`, `scheduleStatus=scheduled`, `active=true`, and does not mutate recurrence definition fields.
    - Test 4: Update applies only when `incoming.updatedAt > existing.updatedAt`; stale/equal updates are no-op and return persisted record.
    - Test 5: Change-event writes occur only for effective state changes and dedupe by noteId/userId/operation/payloadHash.
  </behavior>
  <action>Implement `createRemindersService` with list/get/create/update/delete/ack/snooze methods. Derive mutation identity from authenticated user-facing input (service signatures should accept explicit `userId` and reject cross-user writes). Recompute `nextTriggerAt` via `computeNextTrigger` whenever recurrence definition changes (D-23) and never reimplement recurrence math (D-09). Compute payload hash server-side from normalized reminder state and default missing `deviceId` to `web` (D-16). Preserve immediate post-change hook behavior by invoking an injected `onReminderChanged` callback immediately after successful event append (D-15), with default no-op implementation for this phase.</action>
  <verify>
    <automated>MISSING - Wave 0 must create apps/backend/src/tests/reminders/service.test.ts first</automated>
  </verify>
  <done>Reminder service enforces deterministic transition matrix, strict LWW conflict semantics, and dedupe-aware change-event emission.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add reminder service regression tests for parity-critical transitions</name>
  <files>apps/backend/src/tests/reminders/service.test.ts</files>
  <behavior>
    - Test 1: Invalid timezone on create/update returns validation-class error and no repository mutation.
    - Test 2: Recurrence-definition update recomputes `nextTriggerAt` from shared utility, ignoring conflicting client value.
    - Test 3: Repeated stale updates remain silent idempotent and do not append events.
    - Test 4: Ack/snooze pathways mutate exactly the expected fields and preserve untouched recurrence fields.
  </behavior>
  <action>Create node:test unit-style service coverage with repository doubles (pattern from existing backend tests). Include DST-sensitive cases by stubbing `computeNextTrigger` return values and asserting service delegates recurrence decisions to shared utility rather than local date math. Assert event dedupe tuple behavior and immediate hook callback invocation only on effective state change.</action>
  <verify>
    <automated>npm --workspace apps/backend run build ; node --test "dist/tests/reminders/service.test.js"</automated>
  </verify>
  <done>Service-level reminder parity behaviors are covered by deterministic tests and pass in compiled test runtime.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                     |
| ------------------------------------- | ----------------------------------------------------------------------------------------------- |
| client -> reminder service methods    | Untrusted payload timestamps, timezone, recurrence, and action intent cross into mutation logic |
| reminder service -> SQL repositories  | Business invariants must survive persistence writes and race conditions                         |
| reminder service -> change event hook | Post-change notification hook can amplify bad writes if not gated                               |

## STRIDE Threat Register

| Threat ID | Category | Component                               | Disposition | Mitigation Plan                                                                                                                 |
| --------- | -------- | --------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | T        | `createRemindersService.updateReminder` | mitigate    | Enforce strict LWW (`incoming.updatedAt > existing.updatedAt`) and reject stale/equal writes as no-op per D-17 to D-19          |
| T-04-02   | T        | Recurrence transition logic             | mitigate    | Use only `computeNextTrigger` from shared utility (D-09) and validate timezone at boundary (D-10, D-11)                         |
| T-04-03   | R        | Change-event writes                     | mitigate    | Persist dedupe tuple `(noteId,userId,operation,payloadHash)` and append only on effective state change (D-13, D-14)             |
| T-04-04   | D        | Ack/snooze mutation path                | mitigate    | Keep transitions deterministic, avoid retry loops in this phase, and emit one immediate hook call after successful write (D-15) |
| T-04-05   | E        | Cross-user mutation attempts            | mitigate    | Require user-scoped repository operations and forbid service-level userId mismatches                                            |

</threat_model>

<verification>
- `npm --workspace apps/backend run build`
- `node --test "dist/tests/reminders/service.test.js"`
</verification>

<success_criteria>

- Recurring and one-time acknowledge behavior matches parity matrix for done/snooze/unschedule semantics.
- Snooze updates due-state deterministically without recurrence definition drift.
- Invalid timezone and stale/equal updates do not mutate persisted state.
- Effective writes generate exactly one dedupe-safe change event and call immediate hook once.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-reminder-domain-parity/04-01-SUMMARY.md`
</output>
